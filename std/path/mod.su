// std.path — 路径拼接、分解、规范化（自举前最小实现）
// 自举前：提供最小可调用 API；路径操作后续扩展。

// 占位函数，表示本模块已可 import。
function placeholder(): i32 { return 0; }

// 表示“空路径长度”占位（0），后续扩展为路径拼接与 len。
function path_empty_len(): i32 { return 0; }
