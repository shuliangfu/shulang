// std.runtime — 运行时初始化、panic 钩子等（自举前最小实现）
// 自举前：提供最小可调用 API；panic 钩子等后续扩展。

// 占位函数，表示本模块已可 import。
function placeholder(): i32 { return 0; }

// 表示运行时已就绪（占位），返回 0。
function runtime_ready(): i32 { return 0; }
