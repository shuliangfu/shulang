// std.heap — 堆分配、Box 等（自举前最小实现）
// 自举前：提供最小可调用 API；堆分配与 Box 后续扩展。

// 占位函数，表示本模块已可 import。
function placeholder(): i32 { return 0; }

// 表示“分配大小 0”占位，后续扩展为 alloc(size)/free 等。
function alloc_size_zero(): i32 { return 0; }
