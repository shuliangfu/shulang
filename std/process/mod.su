// std.process — exit、args、环境变量（自举前最小实现）
// exit(code) 终止进程，由 codegen 生成对 C exit() 的调用。

// 终止进程，退出码为 code（noreturn；C 侧调用 exit(code)）
function exit(code: i32): i32 {
  return 0;
}

// 占位，表示本模块可 import；args/环境变量等后续实现
function placeholder(): i32 { return 0; }
