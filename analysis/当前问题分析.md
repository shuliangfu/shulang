# 当前进度与问题分析

> **更新时间**：2026-09-09 午后（**升钉 `d583f61f5`**；cmp 宽度根修 `65d32efb5`；**L8b 航道非确定性**新卡＝当前主问题）
> **分支**：`self-hosting` · **已推 tip（origin）**：**`41dd4e630`**（docs；代码面 `65d32efb5`）
> **产品 L4 放行钉盘**：**`d583f61f5`**（2026-09-09 晨升钉：双端 L4 真冷＋**bstrict 129/129 零红（历史首次）**；前 `b5be5ed97`）
> **权威入口**：日常 L2 `./xbuild l2-matrix`；真冷 L4＝全擦＋`./xbuild bootstrap-driver-seed`＋`XLANG_HOST_CC_SEED_FORCE=1 ./xbuild all`＋全量 bstrict（**不跑** `xlang check`）
> **本文定位**：L8b 航道非确定性建图（§2）＋活跃残卡全景；历史案卷（if-cond 地雷／sqrt+cbrt／fdlibm 移植）已结案入归档，考古 `git show 41dd4e630^:analysis/当前问题分析.md`。

---

## 0. TL;DR（一句话）

**当前主问题＝L8b 航道非确定性**：`labi_prefer_try_x_to_o` 对 `labi_ondemand_list.x` 的发射在**同一重建配方**下产出两种 g15 针表（晨＝28 正确／午后＝24 陈旧），Ubuntu `run-compress` 门禁随之随机红绿（`std_compress_*_stream_state_bytes` UNDEF）；晨间同配方曾达成**双端 L4 真冷＋bstrict 129/129 零红（历史首次）并据此升钉 `d583f61f5`**。午后 cmp 宽度根修（`65d32efb5`）后的两次独立重建均复现 24 表——**非 cmp 修复引入**（源码实证 28、mac 全绿、两者无文件交集），疑 pure-asm thin 子集发射或 `/tmp` 陈旧切片污染 first-wins 合并。

---

## 1. 进度快照（2026-09-09）

### 1.1 已收（本两日）

| 波 | 内容 | SHA |
|----|------|-----|
| std 红簇 12 项全闭 | env PUSH→clz 父链→布局三面→DEREF 按值→返回位 lit 盖章→VAR block_ref→PTR 剥壳＝run-string 绿 | `981ef6056`→`16745b87e`（8 日归档 44 行） |
| 三连闭 | u64 步长 ≤8／core_slice ensure（wipe 自愈）／xlang_sys 针＋fio 推送＝run-slice＋run-process 双端绿 | `f7e9a16a7` |
| labi 多切片谜破解 | 基线＝**意外 full-seed 兜底**（heavy.x fn#142 既有 typeck 债挡 -E→split 永不走）；split 的 -E **不发射** fn#142＝真发射缺口；第二缺口＝build 子命令链路 | `c42c1c1b4`（docs） |
| run-compress 收口 | g15 针表 24→28（L8b `.x` 切片）＋seed 伴块系统 `.so` 推送（full-seed 后备）＝**全量 bstrict 129/129 零红（首次）** | `d583f61f5` |
| 升钉 | 双端 L4 真冷（Ubuntu 15m／mac 16m，bstrict 双零红）→ **`b5be5ed97`→`d583f61f5`** | `e6ce737a8`（docs） |
| cmp 宽度根修 | dep 泛型车道 u64 `t == 0` 曾被打成 32 位比较（旧「u64 宽字面量 dep 实参到 0」卡实为此 bug——实参 movabs 正确）；`pipeline_asm_emit_cmp_elf` 左右两面接 `glue_var_expr_type_ref_with_decl_fallback_c`（binop-shift 同链） | `65d32efb5` |

### 1.2 门禁现状

- **macOS**：L2 5/5＋compress/slice/process/set/string/bitops/env/net/heap/channel 十门禁**全绿**（＠`65d32efb5` 重建）。
- **Ubuntu**：L2 5/5＋slice/process/set/string/bitops/env/net/heap 绿；**run-compress 红**（＝§2 L8b 非确定性卡，非 cmp 回归）。
- **钉据**：`d583f61f5` 晨间双端 L4 零红日志 `/tmp/xlang_l4_Linux_d583f61f5.log`／`/tmp/xlang_l4_Darwin_d583f61f5.log`。

---

## 2. 本案问题地图：L8b 航道非确定性（g15 表 24 vs 28）

### 2.1 现象

- Ubuntu `run-compress` 门禁红：UNDEF `std_compress_{gzip,brotli,zstd}_stream_state_bytes`＋`brotli_stream_init_decompress_`。
- gdb 实证：链上 `labi_od_simple_group_sym_count(15)` 返回 **24**；而源码（`labi_ondemand_list.x:376`＋seed `:196`）**皆 28**。
- **同一配方两种结果**：晨间（08:54–09:09）全擦→`bootstrap-driver-seed`→`FORCE all` 产出**工作表**（bstrict 129/129 零红）；午后两次独立重建（`ensure FORCE`＋完整 L4 配方）均产出 **24 表**。
- macOS 不受影响（run-compress 全绿）。

### 2.2 数据流（谁产生、谁存储、谁消费）

- **产生**：`labi_prefer_try_x_to_o src/runtime/labi_ondemand_list.x`（ensure_host_cc_seed_o.sh:1546）——先 `pure_asm_x_to_o`（`./xlang -backend asm -c`），失败再 `-E`+cc。
- **存储**：mktemp 切片 `/tmp/labi_l8b.XXXXXX` → `pure_ld_partial_merge`（cc -r `--allow-multiple-definition`，**first-wins**）合入 `src/runtime_link_abi.o`。
- **消费**：`labi_od_user_needs_simple_group(user_o, 15)` 逐针查 UNDEF → 命中才走 sg15 ensure＋推送（compress.o／zlib.o／gzip.o＋系统 `.so`）。

### 2.3 已排除（证据）

| 假设 | 排除依据 |
|------|----------|
| 源码漂移 | `grep return 28` 双孪生实证；git 处于 `41dd4e630`（含 `d583f61f5`） |
| cmp 修复引入 | 两者无文件交集（cmp 只动 runtime_pipeline_abi.*）；且 cmp 验证期（重链后）run-compress 曾绿 |
| 针不触发（表对但 U 不在） | UNDEF 在 ld 报错里实证存在；晨间同 UNDEF 曾命中 |
| mac 侧问题 | mac 全绿（brew 航道独立） |

### 2.4 疑点（按序查）

1. **pure-asm thin 子集发射**：独立 `./xlang -backend asm -c list.x` 产物的 sym_count 体内只见 `0x19/0xc/0x0` 等非源码值——**过滤子集**（非全量发射）；其 g15 分支可能整段缺失→合并后 24 来自哪片？
2. **切片胜者漂移**：`labi_l8b` mktemp 内容依 pure-asm 成败而二态（thin 子集 vs full-seed `from_x.c`）；`pure_ld_partial_merge` first-wins，若某轮混入陈旧切片即胜者错。
3. **`/tmp` 陈旧切片污染**：`labi_l8b.*` mktemp 不自清理；午后多次 `git stash`／`git checkout` 窗口可能产出过 24 表切片并滞留 `/tmp`（晨间 L4 前无此窗口）。

### 2.5 收法（下一刀）

1. **先清场**：`rm -f /tmp/labi_l8b.* /tmp/labi_l8c.*` 后完整重建——恢复 28 ⇒ 确认污染源＝陈旧切片；修法＝labi prefer 改用私有 tmpdir 或用后即删。
2. 仍 24 ⇒ 仪器化抓 L8b 两态产物（keepAll 法）＋objdump 反推 24 表真身 → 把 sym_count/sym_at 钉到唯一权威（full-seed 或独立注入切片，学 `field_load_sz_thin` 先例）。
3. 回归集：run-compress＋CNT15 gdb 探针＋全量 bstrict；收后复跑 tip 双端 L4。

---

## 3. 活跃残卡（全景 · 按优先级）

1. **L8b 航道非确定性**（§2）——收后须复跑 tip 双端 L4。
2. **heavy.x fn#142 既有 typeck 债＋-E 发射缺口**：`xlang_asm_ld_append_on_demand_user_objs` 体含 check_block 失败（挡整个 heavy.x 的 -E）；且 split 航道的 -E **不发射该函数**——**动 heavy.x 前必先补 -E 发射**，否则把产品从「意外兜底」翻进「发射缺口」直接断链。
3. **mac scratch 点分 dep 不共发射**（`UN _core_*`，dead_strip 产品面不挡）。
4. **u64 其余比较族扫描**：cmp 根修只证 `==`；`!=`/`<`/`<=` 与切片 `.length` u64 比较待探针扫一遍。
5. **macOS prove `runtime_pipeline_abi` 1832 行符号差**（.x vs surface 既有漂移）；**macOS f-atomic 5 dup**。
6. **陈债不动**（钉盘不升不碰）：Defect D typeck XT001 paren 复合左值；`function(i32):i32` 全局赋值 mismatch（p3 家族）；fnptr-as thin 历史内联 spell；seed 第二 WIN-leftover addr_of 变体；9.4.3 run 转发 v1 规则；fmt FMT001 收集债（check 闸门暂停）；experimental promote 链污染；wpo-full-chain gate macOS 红；10.7.1 va gate 双端红；bridge 4 参 weak 桩先序；退役 mega stale esc_gate；modlet 表 64 项上限；RELA＋字串池。

---

## 4. 方法教训（本两日沉淀，动 labi/pabi 前必读）

1. **三处同 commit**：inject 切片才是链上真权威（`field_load_sz_thin` 先例）——改 `pipeline_expr_field_access_load_byte_sz` 须切片＋mega `.x`＋seed 冷孪生三处；改 `labi_od_*` 表须 list `.x`＋seed 双孪生。
2. **重链次序敏感**：prefer `.o` 改后须**完整 `./xbuild build` 重链**（单跑 `link-product-asm` 会用旧 `xlang`）；stale 二进制窗口会让修复「看起来无效」。
3. **`/tmp` mktemp 切片不自清理**：调试期多次 stash/checkout 会留陈旧切片，可污染 first-wins 合并（＝§2 疑点 3）。
4. **黑盒工具箱**：gdb 条件断点＋`finish`＋`printf` 读返回（针表/计数/walker 触发实证）；`objdump -r` 看 reloc（剥壳是否上链）；`bash -x` ensure 抓两态分支；仪器化 cp 中间产物（keepAll 法）。

---

## 5. 复现与防护命令

```bash
# CNT15 探针（28=对 / 24=错）
gdb -batch -x /tmp/cnt15.gdb --args ./compiler/xlang_asm build -L . tests/compress/main.x -o /tmp/zz
#   /tmp/cnt15.gdb: break labi_od_simple_group_sym_count if $esi==15 / run / finish / printf "CNT15=%d\n",$eax

# run-compress 门禁
bash tests/run-compress.sh

# L8b 产物两态（pure-asm 子集 vs 源码全量）
cd compiler && ./xlang -backend asm -c -o /tmp/l8b_pure.o src/runtime/labi_ondemand_list.x
objdump -d /tmp/l8b_pure.o | sed -n "/<lambi_od_simple_group_sym_count>:/,/^$/p"

# 清切片污染后全链重建（§2.5 第 1 步）
rm -f /tmp/labi_l8b.* /tmp/labi_l8c.* 2>/dev/null
find compiler std core -name "*.o" -delete
rm -f compiler/xlang compiler/xlang_asm compiler/xlang-c compiler/bootstrap_xlangc
./xbuild bootstrap-driver-seed && XLANG_HOST_CC_SEED_FORCE=1 ./xbuild all
```

---

## 6. 结案（cmp 宽度 · `65d32efb5`）

**已闭。** 「u64 宽 INT_LIT 作 dep 调用实参到 0」旧卡**翻案**：实参两侧 `movabs $0x8000000000000000` 位级正确；真凶＝被调方 dep 泛型车道里 `t == 0` 的 EQ 比较被 32 位化（`cmp %eax,%ebx`＋`sete`）——`pipeline_asm_emit_cmp_elf` 的 `is_cmp_64bit` 只认 typeck stamp（dep 无 stamp→0）。根修＝左右操作数 stamp 空且为 VAR 时接 `glue_var_expr_type_ref_with_decl_fallback_c`（param 表／scope 走链，与 binop-shift 宽度同纪律），.x＋seed 冷孪生同 commit。验收：dep `eq64lit`/`clz64(2^63)=0`/use2 clz 矩阵全绿＋双端 L2 5/5＋mac 十门禁绿。§2 L8b 卡独立另收。
