# Xlang / X language 自举进度

> **更新**：2026-09-08 · tip **`f7e9a16a7`**（u64 步长＋core_slice ensure＋xlang_sys 针＝run-slice/run-process 双端绿；前 `0ca0911e1`）（`need_env`＋`bank_save`＋fk0 `args_iter_*_c`）· 前 `1c46bef21`（f64 AAPCS64）· 钉盘 **`b5be5ed97`（双端 L4 129）** · 日切：7 日 **21** 行已入 [归档](自举进度-归档-2026-09-07.md) · **金标 SSH＝`ubuntu-server`** · Ubuntu 验＝**git pull（禁 scp）** · Ubuntu L2 **钉盘节奏**\
> **产品 L4 放行钉盘**：**`b5be5ed97`**（2026-09-02 升钉 · 前 `e8176cbe5` 2026-08-23 · 再前 `f7424ae47` 2026-08-15）\
> **产品 residual tip**：link_abi **7.3.1 关 pin ✅**（12 labi_*.x 切片 prefer；默认 FROM_X=1）· parser **7.2.2 关 pin ✅**（tip `-E` + assemble；产品默认 FROM_X=1）· codegen 7.4.2 wave323 ✅ · typeck 7.4.1 wave322 ✅ · runtime monofile wave321 ✅ · present **0** · 钉盘 **`b5be5ed97`** · Stage 8 tip **wave332 Track L 30/30 FULLY CLOSED**（**PRODUCT HALF=0** · **PRODUCT PINNED=0** · **PRODUCT RETIRED 23/23 = 100%**）· **7.4.3 PREFER_X_O 双端 8/8 wave338 ✅** · **prefer 族 pure-asm 产品默认 ✅** · **pipeline_abi pure-asm 硬禁**\
> **tip L4 安全网（历史）**：**`ffc0b646f`**（已由 tip L4 升钉吸收；前网 `4bacaf18c`）\
> **终局债**：[C迁移追踪.md](C迁移追踪.md)（状态 only · 无波次流水）· [Makefile迁移表.md](Makefile迁移表.md)\
> **本波状态**（一行）：本波主刀＝**三连闭（`f7e9a16a7`）＝run-slice＋run-process 双端绿**：u64 步长 ≤8／core_slice ensure（wipe 自愈）／xlang_sys 针 34→38＋freestanding_io 推送；bstrict 单红 run-compress（zstd/brotli 第三方未建，另卡）；Ubuntu 残 subslice（既有卡）；前波 PTR 剥壳＠`16745b87e`；航道谜底＝链上胜者是 `field_load_sz_thin.x` 注入切片（first-wins T 盖 mega .x 弱版）；前波 VAR block_ref＠`b86679ad9`：rest 航道 walker `glue_fill_var_block_refs_c`（块递归＋表达式树：binop／ASSIGN 28-38／unary／as／field／array／struct-lit／index／call）；gdb 证 VAR 盖章✓、emit 期 base resolved 非零✓；前波返回位 lit 盖章＠`d38be40d6`：rest 航道 C walker `glue_stamp_return_lits_in_block_c`＋typeck 包装器接线（emit 侧三处补章均被 prefer 航道吞掉，gdb 证预跑航道触发）；`std_string_new` 发射修正（length＠0x100）。前波 self-merge sync＠`ad84d5034`（`ec8e47257`）：asm `-o` dep 走 parse-only 预跑无 typeck → dep arena block 父链不绑 → emit 期 scope 回退走不到函数体 `let`，u32 `>>` 误判有符号发 SAR（`core.builtin clz_u32(0x80000000)` 死循环＝CORE-009 挂）。根修＝parse-only／skip_typeck 预跑成功后调 `pipeline_typeck_patch_all_body_parent_links_c`（G.7 typeck.x 权威）。双端 bitops 门禁 OK＋L2 5/5；env/net/heap/channel/json 邻域绿。前波 env PUSH＠`981ef6056`。钉盘仍 `b5be5ed97`。
> **下波候选**：**run-compress（§3.4）**＝asm 直链航道（raw ld）对 zstd/brotli 无 `-l` 旗标面：`-lzstd/-lbrotli*` 推送只挂 C 前端车道（`labi_invoke_cc_list.x:2957`，generated-C 针）；zlib 先例＝X 移植（`std/compress/zlib/libz.x`→zlib.o，无系统依赖）；zstd/brotli `lib.x` 走 extern C FFI（`ZSTD_*`/`BrotliEncoder*`），Ubuntu 系统库在 `/usr/lib/x86_64-linux-gnu/lib{zstd,brotlienc,brotlidec}.so`。两条修法：①按 zlib 先例 X 移植 zstd/brotli（大工程）；②asm-lane need 针（g15 表已扩+实证触发）推系统 .so 绝对路径（ld 可链 .so；须过零 libc/静态产品策略关）。日常闸门＝**双端 L2 产品矩阵**。日常闸门＝**双端 L2 产品矩阵**。日常闸门＝**双端 L2 产品矩阵**。修复须回 `glue_struct_layout_field_offset_by_name_c`／import merge 产生层。另：u64 宽 INT_LIT 作 dep 调用实参到 0。日常闸门＝**双端 L2 产品矩阵**。

> **效率纪律（2026-08-07）**：禁止再开「一 BSS cell 一波」；一波至少一 **域文件** 真减 host-cc LOC 或 present−1；过程上 prefer+heat 一次／域，双端矩阵同波一次\
> **验证纪律**：**自举期暂停 check 闸门**（L2/L4 均不跑 `xlang check`，2026-08-05）；**日常闸门＝SHARED 双端 L2 产品矩阵**（mac + Ubuntu `./xbuild l2-matrix`；wave299：捕获 rv=42／opt=102 成功码，禁 set -e 截断）。leftover-PE unique（`!FROM_X` 或 `WIN_LEFTOVER`）不进 leftover rest FROM_X 产品；leftover-PE 探针红 **不**当双端 L2 产品红。谈自举／升钉才 L4 + 矩阵 + 全量 bstrict；bstrict **per-script timeout** Ubuntu 300s／Darwin L4 1200s + catalog cache；**墙钟**：`product_l4` 分步 wall + `run-all-bstrict`（`XLANG_BSTRICT_TIMING_FILE`）；并行 **`XLANG_BSTRICT_JOBS` 默认 1**（Ubuntu 金标 1；Darwin L4 默认 1／GATE_CASE_JOBS=1；JOBS=8／4／2 本机 OOM）

> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景（换 IDE 续作）**：[自举时序.md](自举时序.md)（S0–S8 先→后 · 30 秒协议）\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[07](自举进度-归档-2026-09-07.md) · [06](自举进度-归档-2026-09-06.md) · [05](自举进度-归档-2026-09-05.md) · [04](自举进度-归档-2026-09-04.md) · [03](自举进度-归档-2026-09-03.md) · [02](自举进度-归档-2026-09-02.md) · [01](自举进度-归档-2026-09-01.md) · [31](自举进度-归档-2026-08-31.md) · [30](自举进度-归档-2026-08-30.md) · [29](自举进度-归档-2026-08-29.md) · [28](自举进度-归档-2026-08-28.md) · [27](自举进度-归档-2026-08-27.md) · [26](自举进度-归档-2026-08-26.md) · [25](自举进度-归档-2026-08-25.md) · [24](自举进度-归档-2026-08-24.md) · [23](自举进度-归档-2026-08-23.md) · [22](自举进度-归档-2026-08-22.md) · [21](自举进度-归档-2026-08-21.md) · [20](自举进度-归档-2026-08-20.md) · [19](自举进度-归档-2026-08-19.md) · [18](自举进度-归档-2026-08-18.md) · [17](自举进度-归档-2026-08-17.md) · [16](自举进度-归档-2026-08-16.md) · [15](自举进度-归档-2026-08-15.md) · [14](自举进度-归档-2026-08-14.md) · [13](自举进度-归档-2026-08-13.md) · [12](自举进度-归档-2026-08-12.md) · [11](自举进度-归档-2026-08-11.md) · [09](自举进度-归档-2026-08-09.md) · [08](自举进度-归档-2026-08-08.md) · [07](自举进度-归档-2026-08-07.md) · [06](自举进度-归档-2026-08-06.md) · [05](自举进度-归档-2026-08-05.md) · [04](自举进度-归档-2026-08-04.md) · [03](自举进度-归档-2026-08-03.md) · [02](自举进度-归档-2026-08-02.md) · [01](自举进度-归档-2026-08-01.md) · …\

### 维护约定

1. 每完成一波 **必改**本文（KPI / 前排 ≤3 / **§6 一行**）；动终局债时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序 · **一行一波** · 描述宜短：做了什么 · tip · 不升钉 · 下一刀）。**禁止**在 §0–§5 堆 `waveNNN` 长清单；细节进 LEAF / C迁移 / 当日归档。
   - **§6 表固定 3 列**：`日期 | 波次 | 描述`。描述列**禁止**裸 ASCII `|`（会把行拆成 4+ 列）；并列名写 `／`、`或`、全角 `｜`，或改写避免。
3. **禁止**把 prove / Stage2 / N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**）。
5. 工程 KPI 写 **SHA + 日志路径**；过期整日流水 **日切归档**后主文档重新起表（见归档头）。
6. 大债地图写 [`当前问题分析.md`](当前问题分析.md)；非每微步。
7. **`当前进度.md` 已删除**（2026-07-30；与本文重复；勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 = `b5be5ed97`**（2026-09-02 升钉 · 前 `e8176cbe5` 2026-08-23 · 再前 `f7424ae47` 2026-08-15）。\
**MG 终局达成 = wave941/942**（Makefile 物理删除 · 0 make in bootstrap · catalog 单权威 · 双端 L4 绿）\
**双端 Stage2 = ✅ SHA256 match**（Ubuntu `9e60e1290` · macOS `343894709` · `xlang_asm_stage1`／`xlang_asm2` bit-identical）\
**选题权威 =** [C迁移追踪.md](C迁移追踪.md) DAG（**变更记录只在本文 §6**）。\
**本波主刀 = env leftover PUSH（`need_env`＋`bank_save`＋fk0 `args_iter_*_c`）**。钉盘仍 `b5be5ed97`。**建图**＝PRIMARY_ENV_OS 同名 `let need` leftover 槽撞 → needs() 写一槽、if 读零槽 → `runtime_env_os.o` 永不入链（Ubuntu UNDEF；mac dead_strip 假绿）。op6 真推后又砸 plan 壳 `bank`（下一 OP_STD env.o → `bank_push` Darwin SEGV）。env_iter：leftover 常 T 发射 `std_env_args_iter_*` 关 fk0，留 U `args_iter_*_c`。根修＝唯一名 `need_env`＋plan 壳 park/restore bank＋fk0 表 11→13＋formal env companion `process_argv`。权威＝`labi_invoke_ld_list`／`labi_ondemand_*` 孪生。验收＝双端 `run-env` 3/3。残：runtime c_face／set abort／string／clz。立卡仍：macOS prove `runtime_pipeline_abi` 1832 行符号差；macOS f-atomic 5 dup；wpo-full-chain／promote／modlet 64／10.7.1 va／bridge 4 参／stale esc_gate／RELA＋字串池／Defect D。

| 指标 | 状态 |
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`b5be5ed97`**；residual tip present **0**；Stage 8 **30/30 FULLY CLOSED**；PRODUCT RETIRED 23/23 |
| Track MG | **11.3.1 MG ✅** |
| Track BC | **🟢 Stage8 30/30** · M4 **5/5** · present **0**；`pipeline_x` mega 仍 host-cc |
| Track L | **收 30/30 FULLY CLOSED** |
| mac / Ubuntu L4（钉盘） | ✅ 双端 L4 + bstrict **129** @ **`b5be5ed97`** |
| pin 蛋 | ✅ darwin.arm64 + linux.x86_64（本波 `xlang_asm`） |
| 本波构建级别 | 钉盘 **L4 已结＠b5be5ed97**；**tip Ubuntu 真冷＠`1c46bef21`**（wipe＋bootstrap-driver-seed＋xbuild all rc=0/0；12 探针 12/12＋L2 5/5 rv42＋bstrict 4m03s 唯一红 run-slice＝既有卡）；**mac arm64 f64 卡四面根修＠`363a68697`＋`1c46bef21`：五红探针全转绿＋L2 rv42**，tip 双端 L4 复跑待排。check 闸门暂停 |

***

## 1. 文档地图

| 文档 | 看什么 |
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [自举时序.md](自举时序.md) | **先→后全景** · 换 IDE 30 秒协议 · S0–S8 主航道 |
| [C迁移追踪.md](C迁移追踪.md) | **终局债 / BC·PC DAG**（状态 only · 无波次流水） |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild 叶映射（MG 已 ✅） |
| [compiler/docs/LEAF_PATTERN_RESIDUAL.md](../compiler/docs/LEAF_PATTERN_RESIDUAL.md) | 叶 residual 人读地图 |
| **本文** | 长期 KPI + 前排 ≤3 + **§6 波次一行记录**（**9 月 8 日**；7 日见 [归档](自举进度-归档-2026-09-07.md)） |
| [自举进度-归档-2026-09-07.md](自举进度-归档-2026-09-07.md) | **9月7日** 9.7.1 FILE\*→fd 统一＋9.5.3 slice2/3a/3b＋9.7.2–9.7.7 系列＋9.6.0–9.6.3 根修＋9.4.2/9.4.3 双端闭环＋9.5.4/9.5.5＋批量收编 docs 行（**21** 行） |
| [自举进度-归档-2026-09-06.md](自举进度-归档-2026-09-06.md) | **9月6日** Cap 9.1/9.4/9.5 全平台闭环＋批量收编 `461834bd1`＋10.6.x Darwin thread/sync/fnptr＋9.5.3 slice3a/b＋Windows MATCH arm BLOCK 前序（**73** 行） |
| [自举进度-归档-2026-09-05.md](自举进度-归档-2026-09-05.md) | **9月5日** leftover unique MATCH dest-parked FIELD／INDEX／DEREF／BLOCK／IF＋双端 L2＠`7290c79e0`（**64** 行） |
| [自举进度-归档-2026-09-04.md](自举进度-归档-2026-09-04.md) | **9月4日** 纯 asm 邻域→ARRAY_LIT TYPE_FN STRUCT_LIT 字段（**40** 行） |
| [自举进度-归档-2026-09-03.md](自举进度-归档-2026-09-03.md) | **9月3日** leftover unique 113→0／P2 Darwin `-o`／windows-gate 全绿（**90** 行） |
| [自举进度-归档-2026-09-02.md](自举进度-归档-2026-09-02.md) | **9月2日** 升钉 `b5be5ed97`／Darwin L4 JOBS／Windows leftover-PE hybrid unique 113→50（**52** 行） |
| [自举进度-归档-2026-09-01.md](自举进度-归档-2026-09-01.md) | **9月1日** typeck pin 7.4.6–8／L4 多轮／Cap va／stage10（**131** 行） |
| [自举进度-归档-2026-08-31.md](自举进度-归档-2026-08-31.md) | **8月31日** stage10 开刀／seed dest-SLICE／leftover／假权威诚实等（**113** 行） |
| [自举进度-归档-2026-08-30.md](自举进度-归档-2026-08-30.md) | **8月30日** leftover XLANG fallthrough F-06／F-07 等（**111** 行） |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图（本波不改） |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | 现行白名单；per-script timeout=300s + catalog cache |
| 产品 L4 放行钉盘 | **`b5be5ed97`** | 2026-09-02 升钉；双端 L4 真冷 + 129 |
| pin 蛋 | **darwin.arm64 + linux.x86_64** | 自钉盘 L4 `xlang_asm` 刷新 |
| residual tip（BC） | present **0** | Stage 8 30/30 · M4 5/5 · prefer pure-asm 默认 · pipeline_abi mega **硬禁** |
| mac 钉盘 L4 | ✅ L4 + bstrict **129** · 137m25s JOBS=1 | `@b5be5ed97` `/tmp/xlang_l4_Darwin_b5be5ed97_jobs1.log` |
| Ubuntu 钉盘 L4（金标） | ✅ L4 + bstrict **129** · 17m47s | `@b5be5ed97` `ubuntu-server` `/tmp/xlang_l4_Linux_b5be5ed97.log` |
| 产品矩阵（日常 L2） | ✅ **`./xbuild l2-matrix`** | 双端 5/5＠`080257c35`（mac／Ubuntu `run-net` 2/2＋fixed_fd 探针；heap／channel／stdtest 邻域不回归） |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 结案 |
| **N** | 111/111 IDENTICAL | 本波不变 |
| **Stage2** | ✅ 双端 tip SHA256 STRICT match | 钉盘历史见归档 |
| **R2 / Cap** | 按需 | 明细见 29／31／1 日归档 |

### 2.3 删 Makefile 闸门（诚实 · MG 已结）

| 键 | 值 |
|----|-----|
| `ENDGAME_PHYSICAL_DELETE_MAKEFILE` | **1** |
| `TREE_ARMED` / `BODY_SHIPPED` / `DELETE_ALLOWED` | **1** / **1** / **1** |
| post-delete residual | **✅** wave944–964 |

***

## 3. 当前前排（≤3 条）

1. **下一刀** — **std 红簇续**：env leftover PUSH 未调 `labi_user_needs_runtime_env_os`（表已齐）／runtime `runtime_crash_evidence_collect_c` c_face／set abort 134／string exit=3／`core_builtin_clz_u32` 挂。本波已闭 leftover skip-emit `read_fixed_fd`／`write_fixed_fd`（stubs weak＠`080257c35`）。前 handle_from_fd＠`37e9088f6`、test＠`d39c57a00`、heap＠`f91e7d85c`、channel＠`767a26088`、json／http／hash＠`9e7dfc8b0`。非 mega、不升钉。
2. **立卡（活跃残）** — **SysV x86_64 8B 结构体 ABI ✅ 已根修＠`b5cf55fd0`**：INTEGER 类 `{ handle: i64 }` 返回／传参 load 位级（Path A0＋use_lea 仅 >16 lea＋named_layout 保留 size=8）；Ubuntu arrow／struct8 rc=0。＋ **mac arm64 f64 错 ✅ 已根修翻案＠`363a68697`（2026-09-08 真冷揭出、同日根修）**：翻案＝非"非确定/跨编译翻转"，确定性单根因＝**arm64 四面 GP-bits 内部调用约定 vs AAPCS64 cc-object 边界**（调方 f64 实参按 IEEE 位送 x0/x1、返回从 x0 收——内部自洽；host-cc seed `.o` 按 AAPCS64 实参 d0-d7／返回 d0——跨界 f64 调用静默返回首参；"跨编译翻转"表象＝stale-.o 不同编译器代次，wipe 前双端绿＝旧 `.o` 同代次自洽）。根修＝四面 AAPCS64 对齐（F1 调方实参分类独立 fp 游标／F2 调方返回收割 kind15 双 arch fmov x0,d0／F3 被调方 v-slot 驻留＋fp_cur＋栈溢出位／F4 返回面单出口转换＋method e-path／UFCS u-path＋隐式尾；f64 only，f32 双侧仍 GP-bits 自洽）；mac 验收＝五红探针全转绿 rc=0（exact7v 96→0／flooronly／fminonly／fminneg2／iso1）＋extdbg/fmindbg/roundneg 逐位对 Python 权威值＋L2 hello rv42／ondemand rv0；跟进刀 `1c46bef21` 补 `-E` 冷路径 extern 声明（mac 曾被纯 asm 直发 `.o` 路径掩盖）；Ubuntu 金标真冷复验＠`1c46bef21`＝12 探针 12/12＋L2 5/5＋bstrict 唯一红 run-slice（既有卡）＝零回归。 **（活跃残余下）** ＋ **macOS prove `runtime_pipeline_abi` 1832 行符号差**（.x vs surface 既有漂移；stash 基线同红；修复须 surface 重 pin 另刀）＋ **macOS f-atomic ordering/widen 红**（`std_atomic_*` 5 dup＝本地 std/atomic/atomic.o 构建态；Ubuntu 金标同门 ok）＋ **Ubuntu std 红簇**（json／http／hash＠`9e7dfc8b0`＋channel fk==7＠`767a26088`＋heap mem_set＠`f91e7d85c`＋test `test_expect_*_c`＠`d39c57a00` 已闭；残 env PUSH／runtime c_face／set abort 134／string／clz；f-simd-v1 构建态；aes-gcm `crypto_mem_eq_c`）。handle_from_fd＠`37e9088f6`＋read／write_fixed_fd＠`080257c35` stubs weak 已闭。
3. **立卡不动（前端/产品面陈债）** — **Defect D** typeck XT001 paren 复合左值（while/return 内误报 implicit tail return）＋ **`function(i32):i32` 全局赋值 typeck mismatch**（p3 家族）＋ **fnptr-as thin 历史内联 spell**（dev box mega -E OOM；Ubuntu hybrid 已吃 .x 权威）＋ **seed 第二 WIN-leftover addr_of 变体**＋ **9.4.3 run 转发 v1 规则**＋ **fmt FMT001 收集债**（check 闸门 2026-08-05 暂停）＋ **experimental promote 链污染产品面**＋ **wpo-full-chain gate macOS 红**＋ **10.7.1 va gate 双端红**＋ **bridge 4 参 weak 桩先序**＋ **退役 mega stale esc_gate**＋ **modlet 表 64 项上限**＋ **RELA＋字串池**。钉盘不升。

***

### 3.1 set abort 134／string exit=3＝import 共发射 struct 布局三面漂移（✅ 根因闭＠`5dc783d96`；残面见下）

**根因（已根修）**：`pipeline_backend_asm_codegen_ast_to_elf_c` 对每个发射模块（含 dep 自身）调 `typeck_merge_dep_struct_layouts_into_entry`；dep 自发射时 m==dm → merge 先 `reset_slot` 清零 dep 自身布局再从已清零槽「拷贝」→ dep 布局 ftr/offset 全被抹 0；entry merge 随后拷走全 0 → 三面（let-init 打包／FIELD 读＝结构体大小盲默认／dep 写＝0）各说各话。根修＝merge dep 循环跳过 `dm == mod`（typeck.x＋typeck_gen seed 同 commit）。验＝三面探针全绿＋24B 按值 `key%cap` 对＋`std.set` 不再 abort＋`std.string` exit 3→4＋双端 bitops OK＋L2 5/5。

**残面（另立卡）**：① ~~dep 内 `*s` DEREF 按值拷贝 CG002~~ **✅ 已闭＠`9b1d575c7`**（`pipeline_asm_push_sysv_memory_by_value_elf_c`／AAPCS64 孪生补 `ko==52`，G.7 复用 `lvalue_eff_addr` DEREF 面；run-set 门禁全绿）；② mac 对 /tmp scratch 点分 dep 不共发射（UN `_core_*`，既有缺口、dead_strip 产品面不挡）；③ u64 宽 INT_LIT 作 dep 调用实参到 0；④ std.string `compare_append_find` exit=4（run-string 残项）。

- **最小复现**（Ubuntu tip `8101be829`）：`core.m6/mod.x` 导出 `struct S6 { keys: *i32, cap: i32, length: i32 }`＋`setcap_readback(s: *S6, c: i32)`＋`readcap(s: *S6)`；main 侧 `let s: S6 = {…}` → `m.setcap_readback(&s, 8)` → 读 `s.cap`。
- **三面 by_name 全 miss＋各自盲默认**（objdump 实证，全 i32 三字段 `S9 {aa,bb,cc}` 即可复现）：main 匿名 STRUCT_LIT let-init 用自身打包计算（0/4/8，i32 时恰正确）；main VAR FIELD 读回退到**结构体总大小**偏移（3×i32 → 0xC，任何字段都读 size 处）；dep 内字段写回退**偏移 0**（写 aa 砸首字段）。dep 内自洽（写读同 0），跨面即错。指针字段时 init 打包也错（*i32 按 4B）。
- **set 链路**：`set.insert` → `slot(*s, key)` 按值拷贝布局垃圾 → `key % s.cap` cap=0 → mod 零 panic「panic: 0」→ abort 134。`with_capacity` 声称成功但主模块读 `s.cap`=0。
- **string exit=3**：`string.length(s)`（String 按值）同族。
- **定位层**：三面（let-init／FIELD 读／dep 写）的 by_name 链在 asm `-o` 共发射下对 import struct 全 miss：dep 模块布局表 parse-only 预跑不填（offset 只由 typeck `pipeline_module_struct_layout_set_field*` 填）；entry 侧 `typeck_merge_dep_struct_layouts_into_entry`（`runtime_pipeline_abi.x:4429`，`all_seeded==0` 分支）理论应同步＋合并，但 FIELD 读仍落盲默认（疑 merge 未及或 entry 侧 lit 布局遮蔽）。修法＝回到布局产生层（dep 预跑后同步 `glue_sync_struct_layout_field_offsets_c`＋merge 一次收三面），禁止再在消费点打第三个补丁。
- **无关已修面**：同模块 local struct 绿；dep 内指针写读自洽；heap alloc 绿。

***

### 3.2 建图：string append SEGV＝dep VAR 无 block_ref → 参数类型回填断链（2026-09-08 诊断，未动手）

- **链路（gdb 实证）**：`s.length`（`s: *String` 参数）FIELD 读发 **64 位 load**（`mov (%rax),%rax`）→ i32 值带相邻 padding 垃圾进 `s + length` 地址算术 → SEGV。
- **逐层根因**：`pipeline_expr_field_access_load_byte_sz` 路径 1（typeck 盖章）dep 无；路径 2（base 布局匹配）需 base VAR resolved 类型——`glue_fill_var_types_from_params_for_func`（emit 期 per-module 已跑、W live）靠 `pipeline_expr_block_ref_at` 向上爬 owner 块，但 **dep VAR expr 的 block_ref==0**（parser 不盖、typeck 才盖；入口模块走路径 1 不需要）→ 爬链断 → base 0 → 默认 8B。
- **试过并回退**：load_byte_sz 路径 2 补 TYPE_PTR 剥壳（.x＋seed）＝无效（base_tr 本来就 0，剥壳够不着）；无独立验证不提交。
- **✅ block_ref 半环已闭＠`b86679ad9`**：rest 航道 walker `glue_fill_var_block_refs_c` 上链（ASSIGN 28-38 家族也走 binop left/right 槽）；gdb 证 VAR 盖章✓、emit 期 base resolved＝param 类型（*S）✓。
- **✅ 全链闭＠`16745b87e`**：剥壳上链后 run-string 门禁绿。**航道谜底**：`load_byte_sz` 的链上胜者＝`pipeline_abi_field_load_sz_thin.x` 注入切片（inject first-wins T，盖过 mega `.x` 弱版）——mega `.x` 单独改永不上链；剥壳须改切片（权威）＋mega 孪生＋seed 冷孪生三处同 commit。同理可推 `_rec` 等其它 inject 切片（`binop_block_peel`／`assign_thin` 等）也有各自的 first-wins 权威。

***

### 3.3 建图：run-slice＝TYPE_SLICE 按值 dep 参数两侧约定不一致（2026-09-08 诊断，未动手）

- **全量 bstrict 现状（std 红簇闭后）**：`XLANG_BSTRICT_JOBS=1` 全量 6m13s，**唯一红＝run-slice**（`length expected exit 0, got 1`）；其余全绿/obs。
- **链路**：`slice.get_i32_unchecked(s,1)` 返回 252（越界读）；`option.some_i32(20)->.value`＝20 ✓（8B Option 按值 OK）；`len_i32` ✓。
- **根因精化（主模块对照＋反汇编）**：约定本身一致＝slice 参数按 host-indirect 传 fat 地址（`glue_func_param_agg_byte_size_c` k==11→8B 指针 home；主模块同构 `get(s,i)` 双端绿：`mov slot; mov (rax)` 先取 &fat 再解引用）。**dep 侧缺的正是那次 `mov (%rax),%rax` 解引用**：被调方（core.slice co-emit）对 `s.data[i]` 的 INDEX 快速路径把参数槽内容直接当 data 指针（`mov slot; lea (rax+i*4)`）。判定原语 `glue_local_var_slot_needs_ptr_load_elf_c`（param 名→pty kind 11→需解引用）已存在且 dep 期可命中，疑其消费面（INDEX/`.data` lvalue 快速路径）在 dep 未咨询或走了不问的分支。
- **修法方向（下一刀）**：找到 dep 侧 `s.data[i]` INDEX 基址快速路径的发射点（emit_index 的 slice-VAR fast path／`.data` lvalue），接上 `glue_local_var_slot_needs_ptr_load_elf_c` 判定（param 槽=&fat → 先 deref）；主模块路径已正确可对照。回归集＝run-slice＋sl4/sl6 探针＋string/set/json 邻域＋L2。

***

### 3.4 建图：run-compress＝asm 直链航道的 zstd/brotli 第三方库接入（2026-09-09 诊断，未动手）

- **现状**：g15 针表已扩（stream_state_bytes 等 4 针）且 gdb 实证触发；UNDEF 残＝`std/compress/{zstd,brotli}` 子模块 `lib.x` 的 extern C FFI（`ZSTD_*`／`BrotliEncoder*/Decoder*`）。
- **`-l` 旗标面缺口**：`-lzstd`／`-lbrotlienc/-lbrotlidec` 只在 C 前端车道推送（`labi_invoke_cc_list.x` generated-C 针）；run-compress 走 asm `-o` 直链（raw `ld.bfd` 对象清单，无 cc 旗标）。
- **zlib 先例**：`std/compress/zlib/libz.x`＝X 移植（自带 zlib.o，零系统依赖）——zstd/brotli 若循此路＝大移植工程（9.2 域「第三方」）。
- **替代**：asm-lane 需要时推系统 `.so` 绝对路径（Ubuntu 有 libzstd.so/libbrotlienc.so/libbrotlidec.so；raw ld 可直接链 .so）——须过零 libc／静态产品策略与 macOS 双端（brew 路径 `ld_append_brew_lib_paths` 已有先例）。
- **回归集**：run-compress＋zlib/gzip 邻域（run-gzip 类）＋L2。
- **2026-09-09 实施受阻记录（诚实）**：sg15 块补推 zlib.o/gzip.o＋系统 `.so`（heavy.x＋seed 双孪生）后 **labi 多切片 prefer 断链**：`runtime_link_abi.o` 丢 `xlang_asm_ld_append_on_demand_user_objs` 定义（pure-asm 与 `-E` 双车道均复现；手编 full-seed 正常含定义）——L8c（heavy.x）切片航道对本次 +30 行编辑敏感的胜者/切片之谜（与 `_rec`/`load_byte_sz` 同族，另一变种）。已回退保双绿；下一刀先解 L8c 切片之谜再落 sg15 推送。

***

## 4. 与归档的关系

- **9月7日归档**：[自举进度-归档-2026-09-07.md](自举进度-归档-2026-09-07.md) — **21** 行 · 9.7.1 FILE\*→fd 统一＋9.5.3 slice2/3a/3b＋9.7.2–9.7.7＋9.6.0–9.6.3＋9.4.2/9.4.3＋9.5.4/9.5.5＋运维两刀 · 钉盘 **`b5be5ed97`**。
- **9月6日归档**：[自举进度-归档-2026-09-06.md](自举进度-归档-2026-09-06.md) — **73** 行 · Cap 9.1/9.4/9.5 全平台闭环＋批量收编 `461834bd1` · 钉盘 **`b5be5ed97`**。
- **9月5日归档**：[自举进度-归档-2026-09-05.md](自举进度-归档-2026-09-05.md) — **64** 行 · leftover unique **0** · 钉盘 **`b5be5ed97`**。
- **9月5日及前**：见 §1 归档链接行。
- **勿**用归档 tip 或旧钉冒充现行 tip L4。**现行产品钉盘 = `b5be5ed97`**。
- 日切后主文档只保留 KPI + 前排 + **9 月 8 日** §6；再堆满则再日切。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
| **macOS arm64** | 钉盘 L4＠**`b5be5ed97` JOBS=1** **`product_l4_true_cold OK` 129** wall **137m25s** · **tip 日常 L2＠`981ef6056`** `run-env` 3/3＋矩阵 5/5＠`4fb63b6fc` · 钉盘 **`b5be5ed97`** |
| **Ubuntu x86_64（金标）** | **tip 日常＠`981ef6056`** `run-env` 3/3（main／env_iter／platform_encoding） · 前 **tip L4 真冷＠`f27479a03` ✅** 12 探针 12/12＋bstrict 128/129（唯一红 run-slice＝既有卡） · SSH＝**`ubuntu-server`** |
| **Windows MSYS2** | leftover unique **0** · min-gate **全绿** · 纯 asm邻域 **6／6**＋call／两 let／赋值／while／for／break／continue／`loop`／match／数组 INDEX／STRUCT_LIT＋FIELD／裸 STRUCT_LIT.field／CALL.field `mk().x`／nested `mk().p.f`／pointer intermediate／METHOD.field `P.mk().x`／9–16B dual-GP CALL.field／sret ＞16B／`take(p: *P)`／`x as i32`／`unsafe { return *p }`／`let y: i32 = *p`／Cap-fn-ptr `foo as *u8`／TYPE_FN `function(): i32` 间接 call／参／字段／ADDR_OF INDEX／FIELD `&a[0]`／`&s.x`／TYPE_FN return `return foo`／TYPE_FN CALL of CALL `getf()()`／nested ADDR_OF `&s.a[0]`／`&a[0].x`／`&*p`／ADDR_OF CALL／STRUCT_LIT 基 `&mk().x`／`&(P { x: 3 }).x`／`&P.mk().x`／ARRAY_LIT TYPE_FN `[foo, bar]`／`fs[0]()`／STRUCT_LIT 字段 `H { fs: [foo] }`／作参 `apply([foo])`／16B `apply([foo, bar])`／TYPE_SLICE `take([foo])`／VAR packing／TYPE_FN let-init COMMON BSS／INDEX TYPE_SLICE 形参 bounds／nso_at 非 RETURN expr-stmt／INDEX TYPE_SLICE 形参／局部 store／ADDR_OF INDEX TYPE_SLICE `&xs[0]`／ADDR_OF INDEX TYPE_SLICE 非字面量 `&xs[i]`／INDEX DEREF `*[]i32` `(*p)[1]`／TYPE_SLICE DEREF 作参 `take(*p)`／CALL 返回 TYPE_SLICE 作参 `take(mk())`／FIELD TYPE_SLICE 作参 `take(s.xs)`／CALL 返回 TYPE_SLICE let-init `let s = mk()`／CALL 返回 TYPE_SLICE let-init 局部 `.length`／ARRAY_LIT TYPE_SLICE 局部 `.length`／TYPE_ARRAY 8B DEREF 作参 `take(*p)` of `*[2]i32`／FIELD TYPE_ARRAY 8B 作参 `take(s.a)`／CALL 返回 TYPE_ARRAY 8B 作参 `take(mk())`／INDEX of `[2][2]i32` 作 TYPE_ARRAY 形参 `take(rows[1])`／FIELD TYPE_ARRAY 16B 作参 `take(s.a)` of `[4]i32`／TYPE_ARRAY DEREF let-init `unsafe { let y: [2]i32 = *p }`／TYPE_ARRAY 整数组赋值 `y = *p`／TYPE_ARRAY dest-in-rbx `*p = a`／TYPE_ARRAY 整数组 return `return a`／TYPE_NAMED dest-in-rbx STRUCT_LIT `*p = P { x: 3, y: 4 }`／TYPE_SLICE dest-in-rbx ARRAY_LIT `*p = [3, 4]`／TYPE_SLICE dest-in-rbx FIELD `*p = s.xs`／TYPE_SLICE dest-in-rbx INDEX `*p = rows[1]`／TYPE_SLICE VAR dest FIELD `b = s.xs`／TYPE_SLICE let-init INDEX `let s = rows[1]`／ADDR_OF INDEX of `[2][]i32` `&rows[1]`／TYPE_SLICE DEREF let-init `unsafe { let s: []i32 = *p }`／TYPE_SLICE VAR let-init `let s: []i32 = xs`／TYPE_SLICE dest-in-rbx VAR `*p = xs`／TYPE_NAMED dest-in-rbx STRUCT_LIT `(*p).y`／**TYPE_NAMED dest-in-rbx MATCH 8B**／**TYPE_NAMED dest-in-rbx MATCH 16B**／**TYPE_NAMED dest-in-rbx MATCH 24B**／**TYPE_NAMED dest-in-rbx nested STRUCT_LIT**／**TYPE_NAMED dest-in-rbx MATCH arm VAR**／**TYPE_NAMED dest-in-rbx MATCH arm CALL**／**TYPE_NAMED dest-in-rbx MATCH arm CALL 24B**／**TYPE_NAMED dest-in-rbx MATCH arm CALL 32B**／**TYPE_NAMED dest-in-rbx CALL**／**TYPE_NAMED dest-in-rbx METHOD**／**TYPE_NAMED dest-in-rbx MATCH arm METHOD**／**TYPE_ARRAY dest-in-rbx MATCH**／**CALL.field 32B** `mk().h`／**VAR dest MATCH arm CALL** `d = match { 1 => mk(); }`／**TYPE_ARRAY dest-in-rbx MATCH arm CALL** `*p = match { 1 => mk(); }`／**TYPE_SLICE dest-in-rbx MATCH** `*p = match { 1 => [3, 4]; }`／**TYPE_SLICE VAR dest MATCH** `d = match { 1 => [3, 4]; }`／**MATCH arm nested MATCH dest-parked**／**MATCH arm STRING_LIT dest-parked TYPE_SLICE**／**MATCH arm STRING_LIT dest-parked TYPE_ARRAY**／**TYPE_ARRAY dest-in-rbx STRING_LIT**／**TYPE_SLICE dest-in-rbx STRING_LIT**／**TYPE_SLICE dest-in-rbx CAST**／**TYPE_SLICE dest-in-rbx IF**／**TYPE_SLICE dest-in-rbx BLOCK**／**TYPE_ARRAY dest-in-rbx CAST**／**TYPE_ARRAY dest-in-rbx IF**／**TYPE_ARRAY dest-in-rbx BLOCK**／**TYPE_NAMED dest-in-rbx CAST**／**TYPE_NAMED dest-in-rbx IF**／**TYPE_NAMED dest-in-rbx BLOCK**／**TYPE_NAMED dest-in-rbx FIELD／INDEX**／**VAR dest TYPE_NAMED CAST**／**VAR dest TYPE_NAMED IF**／**VAR dest TYPE_NAMED BLOCK**／**VAR dest TYPE_NAMED FIELD／INDEX**／**VAR dest TYPE_SLICE STRING_LIT**／**VAR dest TYPE_SLICE CAST**／**VAR dest TYPE_SLICE IF**／**VAR dest TYPE_SLICE BLOCK**／**VAR dest TYPE_NAMED CALL／METHOD**／guarded match／match subject CALL／INDEX／FIELD／const-int guard · 10.1.3 NT **暂缓** |

***

## 6. 变更记录（倒序 · **9 月 8 日**；7 日见 [归档](自举进度-归档-2026-09-07.md)，6 日及以前见 §1 归档行）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-09-09 | **docs(selfhost)** **run-compress 建图＝asm 直链航道第三方库接入（诊断 only）** | **文档 only** · **不升钉** · **无产品面改动**。五问：产生＝`-l*` 旗标只挂 C 前端车道；存储＝zstd/brotli lib.x extern FFI；消费＝asm 直链 raw ld 无旗标面→UNDEF。g15 针表已扩＋触发实证；zlib 先例＝X 移植；替代＝推系统 .so 绝对路径。下一刀二选一（§3.4）。 |
| 2026-09-09 | **fix(selfhost)** **三连闭＝run-slice＋run-process 双端绿（`f7e9a16a7`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。①u64 步长：elem 恢复 `<8`→`≤8`（`[]u64` stride 4→8，last_u64 读错元）；②core_slice ensure：wipe 删 slice.o→needle 静默跳过→UNDEF；照 sys.o 纪律 ensure_formal_make（wipe 实测自愈）；③run-process（昨日被 run-slice 中止遮蔽）：co-emit std.sys.linux 定义 wrapper 留 FFI `xlang_sys_*` U＝真定义者在 `compiler/src/asm/freestanding_io_x86_64.o`（仅主编链）→sys 针表 34→38＋need_any_sys 补推 fio（.x＋seed 双孪生）。验＝双端 run-slice/run-process/set/string/bitops/env/net/heap/channel/json/stdtest 全绿＋L2 5/5；bstrict 单红 run-compress（zstd/brotli 未建）。 |
| 2026-09-08 | **fix(selfhost)** **slice 内建字段类型＋[]u8 步长 — run-slice mac 门禁绿（`0ca0911e1`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝`glue_field_access_field_type_ref_c` 只认 TYPE_NAMED 布局，slice 无布局表→0→deref 决策 miss；消费＝`s.data[i]` 缺 fat 解引用＋`[]u8` 步长默认 4（越界读）。根修＝①resolver 补 slice 内建（`.data`→PTR 驱动 deref、`.length`→usize）；②elem_byte_sz 对裸 PTR 先查 slice 元素尺寸再走泛型剥壳。验＝unchecked i32/u8＝20/15＋length.x 0＋**mac run-slice 门禁绿**＋set/string 等９门禁绿＋L2 5/5；Ubuntu run-slice run=3 残 subslice（baseline 亦红＝既有卡另立）。 |
| 2026-09-08 | **docs(selfhost)** **全量 bstrict 仅剩 run-slice 红＝TYPE_SLICE dep 参数约定不一致建图（诊断 only）** | **文档 only** · **不升钉** · **无产品面改动**。五问：产生＝TYPE_SLICE 按值 dep 参数分类两权威不一致（调用方 host-indirect lea vs 被调方直接指针）；存储＝调用方 arg 分类／被调方 param_home；消费＝`get_i32_unchecked(s,1)`＝252 越界读→length.x exit 1。KPI＝全量 bstrict 6m13s 唯一红。下一刀见 §3.3。 |
| 2026-09-08 | **fix(selfhost)** **load_byte_sz TYPE_PTR 剥壳 — run-string 绿＝std 红簇全闭（`16745b87e`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝VAR block_ref 回填后 base 解析为指针参数类型，但路径 2 只认 TYPE_NAMED；存储＝`pipeline_expr_field_access_load_byte_sz` 路径 2；消费＝i32 字段 8B 默认 load→padding 垃圾进地址（append SEGV／exit=4）。根修＝剥 TYPE_PTR(9) 取 elem；**三处同 commit**＝thin 切片（链上真权威）＋mega `.x` 孪生＋seed 冷孪生。验＝**双端 run-string OK**＋set/bitops/env/net/heap/channel/json/stdtest 八门禁绿＋L2 5/5＋byval/deref 不回归。 |
| 2026-09-08 | **feat(selfhost)** **dep VAR block_ref 回填上链（`b86679ad9`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝parser 只盖块级 stmt 表达式、typeck 才盖嵌套子表达式（dep parse-only 无）；存储＝`W278_Expr.block_ref`；消费＝`glue_fill_var_types_from_params_for_func` 爬链→FIELD base 类型→load 宽度（8B 默认→padding 垃圾进地址）。根修＝rest 航道 walker `glue_fill_var_block_refs_c`（块递归＋表达式树全覆盖：binop4-21／ASSIGN28-38／unary22-24,41,50,51／as／field44／array46／struct-lit45／index47／call48-49）＋typeck 双孪生接线。验＝gdb VAR 盖章✓＋base resolved 非零✓＋双端 L2 5/5＋七门禁绿；残＝load_byte_sz PTR 剥壳不上链（航道谜）→ run-string 仍红。 |
| 2026-09-08 | **docs(selfhost)** **string append SEGV 建图 — dep VAR block_ref 断链（诊断 only）** | **文档 only** · **不升钉** · **无产品面改动**。五问：产生＝parser 不盖 VAR 子表达式 block_ref（typeck 才盖，dep parse-only 无）；存储＝`W278_Expr.block_ref`；消费＝`glue_fill_var_types_from_params_for_func` 爬链＋`load_byte_sz` 路径 2 → 默认 8B load → padding 垃圾进地址。PTR 剥壳试探＝无效已回退（base 0 够不着）。下一刀见 §3.2。 |
| 2026-09-08 | **feat(selfhost)** **dep 返回位匿名 lit 盖章上链 — std_string_new 发射修正（`d38be40d6`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝parse-only dep 预跑无 typeck，返回位匿名 STRUCT_LIT 无类型名盖章；存储＝lit type-name stamp；消费＝字段偏移／存储宽度／返回分类（`struct_lit_value_bytes`）全按字面推断（`data: []`＝16B slice）。根修＝rest 航道新 C walker `glue_stamp_return_lits_in_block_c`（镜像父链 walker 的递归块遍历）＋`typeck_patch_all_body_parent_links`（.x＋typeck_gen 孪生）接线——emit 侧三处补章被 prefer thin(-E)+rest(ld -r)+inject 航道吞掉（链上胜者恒为无章 stub），预跑航道 gdb 实证触发。验＝`std_string_new` length＠0x100 正确＋双端 L2 5/5＋set/bitops/env/net/heap/channel/json 绿＋deref/byval 探针不回归。残＝caller 帧尺寸不足（三个 260B String 局部槽在 rsp 下）＝string 下一刀。 |
| 2026-09-08 | **fix(selfhost)** **self-merge skip 仍 sync dep 布局偏移（`ad84d5034`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝`5dc783d96` 的 skip-self 把 dep 自发射时唯一 sync 机会一并跳过；存储＝dep 布局 parse 期陈旧偏移；消费＝dep 自身字段发射（string cut17 前缀 SEGV 139）。根修＝skip 只跳 reset_slot/拷贝、仍 `glue_sync_struct_layout_field_offsets_c`（typeck.x＋typeck_gen seed 同 commit）。验＝双端 L2 5/5＋bitops/env/net/heap/channel/set 门禁绿＋cut17 SEGV→净退出。残＝string exit=4（返回位匿名 lit 盖章已设计，上链航道待查）。 |
| 2026-09-08 | **fix(selfhost)** **DEREF >16B 按值实参补全 — run-set 门禁全绿（`9b1d575c7`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝`pipeline_asm_push_sysv_memory_by_value_elf_c`（x86）／`store_memory_by_value_to_sp_elf_c`（AAPCS64）只料 VAR/CALL/STRUCT_LIT/FIELD/INDEX，EXPR_DEREF(52) 落未处理 `-1` → `slot(*s,key)` CG002；存储＝两权威分支 4；消费＝`set.insert` 断编＝run-set 红。根修＝G.7 补 `ko==52`（复用 `lvalue_eff_addr` 既有 DEREF 面＝指针位进 rax 即聚合地址）。验＝DEREF 24B 探针 k+cap 对＋**run-set 门禁 OK（abort 134 链全闭）**＋string 推进 exit=4＋双端 bitops/env/net/heap/channel/json 绿＋L2 5/5。残＝string exit=4／mac scratch dep UNDEF／u64 dep 实参。 |
| 2026-09-08 | **fix(selfhost)** **merge 自合并抹布局根修 — 三面漂移闭（`5dc783d96`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝`pipeline_backend_asm_codegen_ast_to_elf_c` 对每模块（含 dep 自身）调 merge，自合并 reset_slot 抹 dep 布局；存储＝dep 布局 ftr/offset→0，entry merge 拷零；消费＝三面（let-init 打包／FIELD 读＝size 盲默认／dep 写＝0）→ set abort 134／string exit 3。根修＝merge 循环跳 `dm == mod`（typeck.x＋typeck_gen seed 同 commit）。验＝Ubuntu 三面探针＋24B 按值全绿＋set 不 abort＋string 3→4＋双端 bitops OK＋L2 5/5＋env/net/heap/channel 邻域绿。残＝DEREF 按值 >16B CG002／mac scratch dep UNDEF／u64 dep 实参。 |
| 2026-09-08 | **docs(selfhost)** **set abort 134／string exit=3 建图 — import 共发射布局三面漂移（诊断 only）** | **文档 only** · **不升钉** · **无产品面改动**。五问：产生＝import merge／caller 布局表（gzip 家族产生层残）；存储＝三面偏移（main let-init 打包 0/4/8、main FIELD 读 0xC、dep store 0）；消费＝`set.insert`→`slot` 按值→mod 零 panic→abort 134；`string.length` 按值→exit 3。同模式爆炸半径＝全部跨模块 struct API（Set／String／Map…）；最小回归集＝三面探针＋run-set／run-string＋L2 矩阵。下一刀：回 `glue_struct_layout_field_offset_by_name_c`／merge 权威一次收三面。 |
| 2026-09-08 | **fix(selfhost)** **dep 预跑父链根修 — bitops clz 挂死闭环（`ec8e47257`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝asm `-o` dep 加载走 `xlang_pipeline_dep_prerun_parse_only_impl`（parse-only 无 typeck）→ dep arena block 父链不绑；存储＝`W277_Block.parent_block_ref`；消费＝emit 期 `glue_var_decl_type_ref_elf_c`→`pipeline_block_resolve_var_type_ref` 父链 walk（入口模块有 full typeck 盖章＋patch 故不触发）。表现＝u32 `>>` 误判有符号发 SAR → `clz_u32(0x80000000)` 死循环（CORE-009 挂）；同块 let 正常、参数正常（param 表不经父链）。根修＝parse-only／skip_typeck 两预跑成功后调 `pipeline_typeck_patch_all_body_parent_links_c`（G.7 typeck.x 权威）；seed 孪生同 commit＋无条件原型。验收＝双端 bitops 门禁 OK（run=2 obs=1）＋`run-builtin` OK＋点分 dep u32/u64 移位探针 rc=0＋双端 L2 5/5＋Ubuntu env/net/heap/channel/json 邻域绿。残＝set abort 134／string exit=3／u64 宽 INT_LIT 作 dep 实参到 0（新立卡）。 |
| 2026-09-08 | **fix(selfhost)** **env leftover PUSH — `need_env`＋`bank_save`＋fk0 `args_iter_*_c`** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2** · ＠**`981ef6056`**（＋style 括号清扫＠`3d99456ef`）。五问：产生＝PRIMARY_ENV_OS 同名 `let need` leftover 槽撞（needs 写／if 读异槽）→ `runtime_env_os.o` 永不推；op6 真推砸 plan 壳 `bank`（下一 env.o `bank_push` Darwin SEGV）；leftover T 发射 `std_env_args_iter_*` 关 fk0 留 U `args_iter_*_c`。根修＝`need_env`＋plan 壳 park/restore＋fk0 11→13＋formal env companion `process_argv`。权威＝`labi_invoke_ld_list`／`labi_ondemand_*` 孪生。验收＝双端 `run-env` 3/3＋mac L2 5/5。下一刀：runtime c_face／set abort／string／clz。 |
| 2026-09-08 | **fix(selfhost)** **std 红簇 leftover skip-emit `std_io_read_fixed_fd`／`write_fixed_fd` — stubs weak 体** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝leftover unique asm `-o` 跳过 std.io 共发射，`std.net.read_fixed`／`write_fixed` 留 U；mac dead_strip 假绿。根修＝G.7 补全 always-on `runtime_asm_io_stubs` weak ≡ `mod.x`（转调 `xlang_io_{read,write}_fixed`＋`_impl`）。勿放 io.o。权威＝`runtime_asm_io_stubs.from_x.c`。验收＝双端探针 rc=0＋`run-net` 2/2＋L2 5/5＠`080257c35`。残＝env PUSH／runtime c_face／set abort／string／clz。 |
| 2026-09-08 | **fix(selfhost)** **std 红簇 leftover `std_io_backend_handle_from_fd` — stubs weak 体（撤回 io.o 针）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝asm `-o` U `std_io_backend_handle_from_fd`（backend.x 不 co-emit）。`5cfdba001` 放 io.o＋g==19 → mac dead_strip 假绿、Ubuntu ctx/error UNDEF。根修＝G.7 补全 always-on `runtime_asm_io_stubs` weak ≡ `backend.x`；撤回 io.o／针。权威＝`runtime_asm_io_stubs.from_x.c`。验收＝mac 探针＋`run-net`＋L2 5/5 无 minos 告警。残＝net read_fixed_fd／write_fixed_fd／env PUSH／runtime c_face／set abort／string／clz。 |
| 2026-09-08 | **fix(selfhost)** **std 红簇 leftover test `test_expect_*_c` UNDEF（`d39c57a00`）** | **代码＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝asm `-o` 共发射 `std_test_*` 为 T，用户 `.o` U `test_expect_c` 族；存储＝L8b leftover `labi_od_test_sym_at`（7 前缀，精确匹配 rest==len 打不中）；消费＝ld 推 test.o。根修＝表 7→28（5 `std_test_*`＋16 mod.x 内层 `*_c`）。权威＝leftover list.from_x.c（链上 full seed）＋list.x／surface 孪生。验收＝双端 `run-stdtest` rc=0＋L2 5/5；json／hash／channel／heap 邻域不回归。Ubuntu env 仍 UNDEF＝另层（PUSH 未调 needs）。残＝skip-emit handle_from_fd／env PUSH／runtime c_face／set abort／string／clz。 |
| 2026-09-08 | **fix(selfhost)** **std 红簇 heap `std_heap_mem_set` UNDEF（`652350bf0`＋`f91e7d85c`）** | **代码＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝asm `-o` 共发射 libc／alloc 为 T，`core_mem_mem_zero` U `std_heap_mem_set`；存储＝L8b leftover `labi_od_heap_api_sym_at`（30 针无 mem_set）＋`provides_std_heap` 两探针挡推；消费＝ld 推 heap.o。根修＝表 30→32＋needs 命中仍推（`--allow-multiple-definition`）。权威＝leftover list.from_x.c（链上 full seed）＋list.x／surface／heavy 孪生。验收＝双端 `run-heap` rc=0＋L2 5/5；json／hash／channel 邻域不回归。Ubuntu set abort 134＝另层。残＝skip-emit handle_from_fd／env·test·runtime 针／set abort／string／clz。 |
| 2026-09-08 | **fix(selfhost)** **std 红簇 bare `*_c` UNDEF — channel fk==7（`767a26088`）** | **代码＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝asm `-o` 共发射 `std_channel_*` 为 T，L8b leftover fk==7 仅 4 针；存储＝`labi_std_fk_gate_sym_at` fk==7（链上＝leftover `labi_ondemand_list.from_x.c`，`.x` 10 针未入链）；消费＝ld 推 channel.o＋glue。根修＝补全 19 针（8 `std_channel_*`＋`channel_send/recv`＋9 `channel_i32_*_c`）。权威＝heavy.x＋list／surface 孪生。验收＝双端 `run-channel` rc=0＋L2 5/5；json／hash 邻域不回归。残＝skip-emit handle_from_fd／heap mem_set／env·test·runtime 针／string／clz。 |
| 2026-09-08 | **fix(selfhost)** **std 红簇 bare `*_c` UNDEF — json／boundary／http／hash（`1bdd2f7c3`＋`9e7dfc8b0`）** | **代码＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝asm `-o` 共发射 mod.x 包装为 T，fk0／fk 精确针只列 `std_*` 公 API；存储＝`labi_fk0_sym_at`／`labi_std_fk_gate_sym_at`（Ubuntu 链上＝L8b leftover `labi_ondemand_list.from_x.c`）；消费＝ld `--allow-multiple-definition` 推 formal `.o`。根修＝补 json `json_parse_*_c` 族＋hash sip/xxhash＋fk==13 `http_get_c`。权威＝`labi_ondemand_heavy.x`＋list／surface 孪生。验收＝Ubuntu json／boundary／http／hash rc=0＋双端 L2 5/5。残＝net skip-emit／heap mem_set／env·channel·test 针／string／clz。 |
| 2026-09-08 | **docs(selfhost)** **9.2 域 standing 收官勘正 — 阶段 9 全系列 ✅** | **文档 only** · **不升钉** · **无产品面改动**。五问：产生＝仪表盘 9.2.2／9.2.5／9.2.6 仍 🟡，但产品路径已双端 L2 绿且裁定不迁库；存储＝C迁移 9.2 行／阶段 9 仪表盘；消费＝选题 DAG。勘正＝三条标 ✅ standing（zlib Init2≡9.3.2／SIMD≡9.3.5／sqlite rest）；popen nm 改标 PC／host（非 9.2 第三方）；13.1.3 ✅。权威＝`C迁移追踪.md` 阶段 9。下一刀：Ubuntu std bstrict 红簇 21 脚本单根。 |
| 2026-09-08 | **feat(selfhost)** **9.2.4 exact-7 宿主 libm splice 清零 — 双端 L2（`c3c9d34c9`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝FROM_X rest 仍定义 `math_{floor,ceil,trunc,round,fabs,fmin,fmax}_impl` 拼宿主 libm，rem_pio2 孪生调 `floor`／`fabs`；存储＝seed／surface TU；消费＝产品 `runtime_math_libm.o` 入链仍拖 `<math.h>`。根修＝删七面 splice＋丢 `<math.h>`；rem_pio2 复用 `math_floor_c`／`math_fabs_c`（G.7＝.x）；surface 位级孪生同语义。权威＝`runtime_math_libm.x`＋from_x／surface 孪生。验收＝mac／Ubuntu `exact7_rc` 30 例 rc=0＋trig／erferfc 邻域 rc=0＋双端 L2 5/5＋nm 零 host-libm UNDEF。fenv＝standing C 桥。下一刀：9.2 standing 收官勘正。 |
| 2026-09-08 | **fix(layout)** **gzip Init2 产品 import `*T.field` offset — 双端 L2 收口（`3a764a61c`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝`for_var_base_field` 在 caller layout 命中但 by_name 返回 -1 时直接放弃（import merge 把 type_ref 重映射成 0，compute 全 0）；存储＝caller 合并表／dep 定义模块+arena；消费＝产品共发射 `clear_alloc`／`deflateInit2_` zalloc。根修＝①var_base miss 走 dep `by_name(dm, darena)`；②typeck j>0 stored 0＝miss 后 dep compute。权威＝`runtime_pipeline_abi.x`＋from_x 孪生／`typeck.x`＋`typeck_gen.linux.x86_64.c`。验收＝mac／Ubuntu `gzip_rc` rc=0＋Ubuntu objdump `add $0x40/$0x48/$0x50`＋`zlib_rc` rc=0＋双端 L2 5/5。下一刀：9.2.4 libm 未移植 residual。 |
| 2026-09-08 | **fix(layout)** **gzip Init2 字段 offset — import 共发射仍立卡（`f86635139`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝产品 `import("std.compress.gzip")` 共发射把 `ZStream` 字段 store 写成 offset 0（standalone `libz.x`／同文件探针 offset 正确）；存储＝import merge 后 caller 布局表；消费＝`gzip_zstream_clear_alloc`／`deflateInit2_` zalloc。本波：①`glue_struct_layout_field_offset_by_name_c` prefer stored、j>0 双 0→-1；②typeck merge 前 `glue_sync(dep, dep_arena)`；③needs_zlib 补 Init2＋Darwin gzip mangle。验收＝mac `gzip_rc`／`zlib_rc` rc=0＋双端 L2 5/5；Ubuntu `zlib_rc` rc=0、`gzip_rc` 仍 SEGV（clear_alloc 仍 `(%rbx)+0`）。下一刀：产品 import `*T.field` emit 仍丢 offset。 |
| 2026-09-08 | **feat(selfhost)** **9.2.2 zlib — extern -lz 产品路径（`49571991a`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。勘正＝不迁 zlib；C 桥 Init2 属 9.3.2（standing）。五问：产生＝g15 只推 facade c_face stub（return -1；既有 gzip_roundtrip `n<=0` 假绿）＋exact 表无 `std_compress_zlib_deflate`；存储＝formal_mod／ld argv；消费＝产品 `-o`。根修＝g15 24 针＋ELF 裸 compress2＋catalog `zlib.o`／`gzip.o`（mod＋libz ＋objcopy leaf）＋g15 companions。gzip Init2 Ubuntu：①named const -1 作参读未写栈槽（`0-1` 修）②x86_64 `ZStream` 字段 store 全 offset 0 → zalloc 垃圾 → `deflateInit2_` SEGV（立卡，非本刀）。权威＝`labi_ondemand_list` 孪生＋`xlang_compile_std_module.sh`。验收＝mac／Ubuntu `tests/probes/wave922/zlib_rc.x` rc=0＋双端 L2 5/5＠`49571991a`。下一刀：x86_64 allow(padding) 字段 offset。 |
| 2026-09-08 | **feat(selfhost)** **9.2.6 sqlite3 C API — extern 系统库 standing rest（`d0085c04d`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。勘正＝不迁 sqlite3；C rest 因 `sqlite3.h`／`SQLITE_TRANSIENT`。五问：产生＝库 TU `let p:*T=&bytes[0]` 落 BSS 0（asm bake 无 ADDR_OF reloc；无 main 不跑 hoist seed）＋fk10 exact 表无 `db_open_c`（Linux 用户 TU 嵌 mod.x wrapper）＋`ld -lsqlite3` 需 -dev；存储＝`g_db_last_err`／link argv；消费＝`db_clear_err`／产品 `-o`。根修＝`db_err_slot`（G.7＝`db_stmt_cache_slot`）＋fk10 33 针＋LINUX `-l:libsqlite3.so.0`。权威＝`runtime_sqlite_glue` rest＋`std/db/sqlite/sqlite.x`＋`labi_ondemand_heavy`／`labi_invoke_ld_list` 孪生。验收＝mac／Ubuntu `tests/probes/wave926/sqlite3_rc.x` rc=0＋双端 L2 5/5＠`d0085c04d`。下一刀：9.2.2 zlib。 |
| 2026-09-08 | **fix(selfhost)** **SysV INTEGER 8B 结构体返回／传参 — load 位级非 lea（`b5cf55fd0`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝`glue_type_named_layout_size_any_module` 把 size_simple=8 丢成 0、STRUCT_LIT value_bytes=0、use_lea(sz==0) lea 槽；存储＝`ArrowColumn.handle`；消费＝`arrow_column_destroy_c` 把栈地址当 heap。根修＝named_layout 保留 ≤8、use_lea 仅 >16 lea、STRUCT_LIT 收包 load 8、return Path A0 load 局部。权威＝`runtime_pipeline_abi.x`＋leftover 孪生＋arrcopy-thin（G.7）。验收＝mac struct8＋arrow＋L2 5/5；Ubuntu FORCE pabi mega＋g05＋struct8 rc=0＋arrow rc=0＋L2 5/5。探针 `tests/probes/wave925/struct8_abi_rc.x`。下一刀：9.2.6 sqlite3。 |
| 2026-09-08 | **test(selfhost)** **双端 L2＠`7c1b71306` — 9.2.4 绿／9.2.5 Ubuntu 红立卡** | **测试＋文档** · **不升钉** · **构建级别 L2**（Ubuntu g05 重建 parser_asm_thin_glue；非 tip L4）。mac：wave924 9 探针＋g5＋arrow 15 例＋L2 5/5 全绿 `/tmp/mac_l2_7c1b71306.log`。Ubuntu：同 SHA pull＋wipe 相关 `.o`＋g05；libm `-E` typeck OK；wave924 **9/9**＋g5 hang-fix **rc=0**＋L2 **5/5**；**arrow 红** run abort `free(): invalid pointer` rc=134。五问：产生＝SysV x86_64 对 `struct { i64 }` 返回局部槽地址／调用方 `lea` 传 wrapper 指针；存储＝`ArrowColumn.handle` 槽；消费＝`arrow_column_destroy_c` 把栈地址当 heap header。对照＝Darwin `new_f32` 以 x0 返回 handle 位。非 SIMD `_impl`、非 32B pack、非 posix_memalign stub（二进制 U libc）。禁改探针期望糊绿。下一刀：该 ABI 根修。 |
| 2026-09-08 | **feat(selfhost)** **9.2.5 arrow SIMD kernels — 勘正并条 9.3.5 standing C 桥** | **测试＋文档** · **不升钉** · **建图**＝四 kernel `_impl`（`arrow_f32_sum`／`dot`／`i32_sum_valid`／`f32_sum_valid`）在 `runtime_arrow_simd_glue` rest，需 SSE2／NEON C target attributes；thin `.x` 已供 wrapper；无胶层 libc；非 Apache Arrow／libarrow（`mod.x` 头注释过时）。与 9.3.5 同桥，语言 SIMD 内建未立前 C 即长期权威（G.7 不另开标量第二套）。**验收（mac）**＝`tests/probes/wave925/arrow_kernel_rc.x` build+run rc=0（15 例：n=4／8 向量宽、n=1／3／5 标量尾、i32／f32 null-bitmap）；`run-simd-s1-gate` run=2；kv-arrow run=3；L2 5/5。Ubuntu 真冷随 Wave A 收口。下一刀：9.2.6 sqlite3。 |
| 2026-09-08 | **feat(selfhost)** **9.2.4 erf／erfc — fdlibm s_erf.c／s_erfc.c 全量 .x 端口＋seed/surface 孪生** | **代码＋测试** · **不升钉** · **权威**＝`runtime_math_libm.x` `math_erf_c`／`math_erfc_c`（G.7 共享 `math_erf_pq`／`paqa`／`rasa`／`rbsb` 四条有理式＋`math_erf_exp_tail` 双 exp 尾；复用 `math_exp_c`／`math_fabs_c`／`math_trig_with_lo`；erf 的 1/0.35 阈＝`0x4006DB6E`、erfc＝`0x4006DB6D`，fdlibm 原文勿合并；erfc 非 naive `1-erf`）。seed 冷孪生 `runtime_math_libm.from_x.c`（`#ifndef XLANG_RUNTIME_MATH_LIBM_FROM_X`）＋surface 镜像同 commit 同语义；`math_erf_impl`／`math_erfc_impl` libm 拼接删除。独立 C 孪生 vs 宿主：多数 EQ，若干 DIFF 1 ulp（钉 fdlibm：erf(1)／erf(0.25)／erf(1e-10)／erfc(1)／erfc(0.7)／erfc(6)）；渐近 `erfc(2)`／`erfc(10)` 再偏 1 ulp 因产品用 fdlibm exp 而非宿主 exp。**验收（mac）**＝`tests/probes/wave924/erferfc_rc.x` build+run rc=0（30 例；非整数输入 bit-pun，含区间边界 0.84375）；atan2／invtrig／pow／trig／expm1log1p／sqrtcbrt／explog／g5 邻域 rc=0；`runtime_math_libm.x -E` parse+typeck OK 0.17s；FROM_X rest 仅 U `math_erf_c`／`math_erfc_c`；L2 5/5。Ubuntu 真冷随 Wave A 收口。下一刀：9.2.5 arrow SIMD。 |
| 2026-09-08 | **feat(selfhost)** **9.2.4 atan2 — fdlibm e_atan2.c 全量 .x 端口＋seed/surface 孪生** | **代码＋测试** · **不升钉** · **权威**＝`runtime_math_libm.x` `math_atan2_c`（G.7 复用 `math_atan_c`＋`math_trig_hi`／`lo`／`with_hi`；fabs 后置故用 with_hi 清 `y/x` 符号位；tiny=1e-300 bit-pun）；seed 冷孪生 `runtime_math_libm.from_x.c`（`#ifndef XLANG_RUNTIME_MATH_LIBM_FROM_X`）＋surface 镜像同 commit 同语义；`math_atan2_impl` libm 拼接删除。独立 C 孪生 vs 宿主：象限／±0／±inf／NaN 全 EQ；`atan2(1,10)` DIFF 1 ulp（钉 fdlibm `0x3fb983e282e2cc4d`）。fdlibm 不区分 `atan2(+y,-0)` 与 `+0`（钉 fdlibm = ±pi/2）。**验收（mac）**＝`tests/probes/wave924/atan2_rc.x` build+run rc=0（30 例；非二进输入 bit-pun）；invtrig／pow／trig／expm1log1p／sqrtcbrt／explog／g5 邻域 rc=0；`runtime_math_libm.x -E` parse+typeck OK 0.17s；FROM_X rest 无 atan2 符号；L2 5/5。Ubuntu 真冷随 Wave A 收口。下一刀：erf／erfc。 |
| 2026-09-08 | **feat(selfhost)** **9.2.4 反三角 asin／acos／atan — fdlibm e_asin.c／e_acos.c／s_atan.c 全量 .x 端口＋seed/surface 孪生** | **代码＋测试** · **不升钉** · **权威**＝`runtime_math_libm.x` `math_asin_c`／`math_acos_c`／`math_atan_c`（G.7 复用 `math_trig_hi`／`lo`／`with_hi`／`with_lo`／`math_sqrt_c`；共享 `math_asin_rational` R(t)=p/q；fabs 后置故用 with_hi 清符号位）；seed 冷孪生 `runtime_math_libm.from_x.c`（`#ifndef XLANG_RUNTIME_MATH_LIBM_FROM_X`＋`#pragma STDC FP_CONTRACT OFF`）＋surface 镜像同 commit 同语义；`math_asin_impl`／`math_acos_impl`／`math_atan_impl` libm 拼接删除。独立 C 孪生 vs 宿主：多数 EQ，若干 DIFF 1 ulp（钉 fdlibm：asin(±0.5)／acos(0.5)／acos(0.999)／atan(±0.5)／atan(2)／atan(10)）。**验收（mac）**＝`tests/probes/wave924/invtrig_rc.x` build+run rc=0（30 例；非二进输入 bit-pun，避 X 词法器 0.7 偏 1 ulp 立卡）；pow／trig／expm1log1p／sqrtcbrt／explog／g5 邻域 rc=0；`runtime_math_libm.x -E` parse+typeck OK 0.18s；L2 5/5。Ubuntu 真冷随 Wave A 收口。下一刀：atan2。 |
| 2026-09-08 | **feat(selfhost)** **9.2.4 pow — fdlibm e_pow.c 全量 .x 端口＋seed/surface 孪生** | **代码＋测试** · **不升钉** · **权威**＝`runtime_math_libm.x` `math_pow_c`（复用 `math_trig_hi`／`lo`／`with_hi`／`with_lo`／`math_trig_scalbn`／`math_sqrt_c`）；seed 冷孪生 `runtime_math_libm.from_x.c`（`#ifndef XLANG_RUNTIME_MATH_LIBM_FROM_X`＋`#pragma STDC FP_CONTRACT OFF`）＋surface 镜像同 commit 同语义；`math_pow_impl` libm 拼接删除。独立 C 孪生 vs 宿主：整数幂／0.5 开方／inf／0 全 EQ；`0.7**1.3` DIFF 1 ulp（钉 fdlibm）；`+-1 ** +-inf` fdlibm＝NaN（宿主＝1，钉 fdlibm）。**验收（mac）**＝`tests/probes/wave924/pow_rc.x` build+run rc=0（25 例）；sqrtcbrt／explog／expm1log1p／trig／g5 邻域 rc=0；`runtime_math_libm.x -E` parse+typeck OK 0.18s；L2 5/5。Ubuntu 真冷随 Wave A 收口。下一刀：asin／acos／atan。 |
| 2026-09-08 | **feat(selfhost)** **9.2.4 三角 sin/cos/tan — fdlibm s_sin.c／s_cos.c／s_tan.c＋k_sin／k_cos／k_tan＋e_rem_pio2／k_rem_pio2 全量 .x 端口＋seed/surface 孪生** | **代码＋测试** · **不升钉** · **权威**＝`runtime_math_libm.x` `math_sin_c`／`math_cos_c`／`math_tan_c`（内核＋`math_rem_pio2`／`math_kernel_rem_pio2`）；seed 冷孪生 `runtime_math_libm.from_x.c`（`#ifndef XLANG_RUNTIME_MATH_LIBM_FROM_X`＋`#pragma STDC FP_CONTRACT OFF`）＋surface 镜像同 commit 同语义；`math_sin_impl`／`math_cos_impl`／`math_tan_impl` libm 拼接删除。独立 C 孪生 vs 宿主：sin 全 EQ／cos 2 DIFF（1 ulp：±0.1）／tan 若干 1 ulp（钉 fdlibm：tan(1)／tan(0.1)／tan(10)）。**验收（mac）**＝`tests/probes/wave924/trig_rc.x` build+run rc=0（30 例）；sqrtcbrt／explog／expm1log1p／g5 邻域 rc=0；`runtime_math_libm.x -E` parse+typeck OK 0.11s；L2 5/5。Ubuntu 真冷随 Wave A 收口。下一刀：pow。 |
| 2026-09-08 | **feat(selfhost)** **9.2.4 expm1+log1p — fdlibm s_expm1.c / s_log1p.c 全量 .x 端口＋seed/surface 孪生** | **代码＋测试** · **不升钉** · **权威**＝`runtime_math_libm.x` `math_expm1_c` / `math_log1p_c`；seed 冷孪生 `runtime_math_libm.from_x.c`（`#ifndef XLANG_RUNTIME_MATH_LIBM_FROM_X`＋`#pragma STDC FP_CONTRACT OFF`）＋surface 镜像同 commit 同语义；`math_expm1_impl` / `math_log1p_impl` libm 拼接删除。独立 C 孪生 vs 宿主：expm1 39 EQ / 3 DIFF（1 ulp）／log1p 33 EQ / 1 DIFF（1 ulp）；产品 rc 探针钉 fdlibm（expm1(1)＝`0x3ffb7e151628aed2`／log1p(2)＝`0x3ff193ea7aad030a`）。**验收（mac）**＝`tests/probes/wave924/expm1log1p_rc.x` build+run rc=0（30 例）；sqrtcbrt_rc／explog_rc／g5 邻域 rc=0；`runtime_math_libm.x -E` parse+typeck OK 0.12s；nm 仅 `math_expm1_c`/`math_log1p_c` 无 `_impl`；FROM_X rest 对两面为 U；L2 5/5 SHA 本波。Ubuntu 真冷随 Wave A 收口。下一刀：三角／pow。 |
| 2026-09-08 | **feat(selfhost)** **9.2.4 sqrt+cbrt — fdlibm e_sqrt.c / s_cbrt.c 全量 .x 端口＋seed/surface 孪生** | **代码＋测试** · **不升钉** · **权威**＝`runtime_math_libm.x` `math_sqrt_c` / `math_cbrt_c`；seed 冷孪生 `runtime_math_libm.from_x.c`（`#ifndef XLANG_RUNTIME_MATH_LIBM_FROM_X`＋`#pragma STDC FP_CONTRACT OFF`）＋surface 镜像同 commit 同语义；`math_sqrt_impl` / `math_cbrt_impl` libm 拼接删除。sqrt 正确舍入（独立 C 孪生 25/25 位等于宿主 libm）；cbrt pin fdlibm（pi 距宿主 1 ulp 钉入探针）。**根修过程发现**：组装指数偏置曾误写 `1073741824`（＝`0x40000000`）注释却标 `0x3fe00000`，结果整体 ×4；Python 验后改为 **1071644672**。**验收（mac）**＝`tests/probes/wave924/sqrtcbrt_rc.x` build+run rc=0（30 例）；explog_rc／g5 邻域 rc=0；`runtime_math_libm.x -E` parse+typeck OK 0.06s；nm 仅 `math_sqrt_c`/`math_cbrt_c` 无 `_impl`；L2 5/5 SHA 本波。Ubuntu 真冷随 Wave A 收口。下一刀：expm1+log1p。 |
| 2026-09-08 | **style(lang)** **产品 if/while 强制包裹括号 `if (cond)`** | **源＋文档** · **不升钉** · **不改 parser**。产品写法统一 `if (cond) {`／`while (cond) {`；子表达式分组写 `if ((x >> 2) != 4) {`。禁止 `if m == 1 {` 与 `if (primary) binop {`。parser 仍接受 wave361/650（扫句器回归探针故意保留 g5 形态）。docs/03 锁定。余点已包：`runtime_math_libm.x` 80／`explog_rc.x` 50／runtime_{log,random,thread,time} 11。下一刀：9.2.4 sqrt+cbrt seed 孪生。 |
| 2026-09-08 | **fix(parser)** **if-cond `(primary) binop` 扫句器死循环 — 内存爆炸根修** | **代码＋测试** · **不升钉** · **根因**＝`parser_asm_scan_sync_after_if_stmt_c` 仍把 `if (` 当整段条件，只跳第一对括号；`if (m + 1) == 1 {` 在 `==` 处找不到 `{` → 退回 if 起点；后随 IDENT 赋值让 `realign` 回扫同一 if → 外层无限重解析、arena ~500MB/s。binop peek-cache 单圈前进正确（H1 证伪）。**根修（G.7）**＝取消 paren-form 特判，`if` 后一律括号深度 0 找 then `{`（与 wave361/650 parse_cond 不剥前导 `(` 对齐）。**验收（mac）**＝g5 `-E`/`build`/`run` rc=0；`(m==1)`／`(m&1)==1`＋return／`(x>>2)!=4`＋赋值 全绿；WIP `runtime_math_libm.x -E` typeck OK；L2 5/5。探针 `tests/probes/wave924/if_paren_binop_assign.x`。下一刀：9.2.4 sqrt+cbrt seed 孪生。 |
| 2026-09-08 | **fix(selfhost)** **mac arm64 f64 卡四面根修 — GP-bits→AAPCS64 边界对齐（`363a68697`＋`1c46bef21`）** | **代码＋测试** · **不升钉** · **翻案**＝非"非确定/跨编译翻转"，确定性单根因＝arm64 后端把 f64 实参按 IEEE 位送 GP 寄存器（x0/x1）、返回从 x0 收（内部自洽 GP-bits 约定），host-cc seed `.o` 按 AAPCS64（实参 d0-d7／返回 d0）——跨界 f64 调用静默返回首参；"翻转"表象＝stale-.o 不同编译器代次。**根修四面**（f64 only；f32 双侧仍 GP-bits 自洽）：F1 调方实参分类（独立 fp 游标，f64 不占 x 槽）／F2 调方返回收割（kind15 双 arch fmov x0,d0）／F3 被调方参数驻留（v-slot＋fp_cur＋栈溢出位）／F4 返回面单出口转换（return_elf_impl 公共尾＋backend 隐式尾）＋method e-path／UFCS u-path 开口。`.x` 权威 3 文件（backend_call_dispatch.x／runtime_pipeline_abi.x／backend.x）＋seed 孪生 2 文件同 commit 同语义。**跟进刀 `1c46bef21`**＝runtime_pipeline_abi.x 补 `backend_enc_mov_rax_to_xmm_arg_reg_arch` extern 声明（363a68697 漏声明；mac 纯 asm 直发 `.o` 侥幸过、Ubuntu 冷启 `-E` typeck XT001 拒绝→pabi hybrid thin 失败→全 seed 回退撞既有 758 错→ensure_prereqs 52/53 FAIL；补声明后 -E parse OK 2526 funcs／typeck OK）。**验收（mac arm64）**＝重建绿；五红探针全转绿 rc=0（exact7v 96→0／flooronly／fminonly／fminneg2／iso1）；extdbg 跨界 f64 全对（fmin 1.5／fabs 2.5／pow·atan2 逐位 Python 验算）；fmindbg 5/5；roundneg trunc/round/frac 位级正确；L2 hello rv42／ondemand rv0。**验收（Ubuntu x86_64 金标＠`1c46bef21`）**＝git pull＋全擦两段式真冷重建 rc=0/0→**12 探针 12/12**＋L2 **pass=5 fail=0**（rv42/opt102/hello0/si0/f32）＋bstrict 4m03s 唯一红 run-slice（既有卡，与 `f27479a03` 基线同）＝**零回归** `/tmp/ubuntu_verify_1c46bef21.log`。 |
| 2026-09-08 | **test(selfhost)** **双端 L4 真冷收口＋运维三发现＋mac arm64 f64 既有卡立卡（`f635c82dd`）** | **测试＋代码（mk）** · **不升钉** · **Ubuntu 金标 L4 真冷＠`f27479a03` ✅**：全擦 compiler/std/core `.o`＋四二进制 → `./xbuild bootstrap-driver-seed`＋`XLANG_HOST_CC_SEED_FORCE=1 ./xbuild all` → **12 探针 12/12**（9.2.4 NaN/f64 矩阵＋fk9 门探针）＋L2 rv42＋bstrict **128/129**（唯一红 run-slice＝既有卡；4m21s）。**发现一（iso1 假红根因）**＝**stale product `.o` 陷阱**：`.o` 陈旧判定只比 `.x` 源时间戳，编译器发射修复（Fix E）不失效旧 `runtime_math_libm.o`（旧编译器 movd 截断 f64 仍在链上；round(-2.5)→-1.0）；删 `.o` 后 ensure 用新编译器重建即绿；**L4 全擦为唯一防**（AGENTS 铁律再实证）。**发现二**＝L4 参考 `make bootstrap-driver-seed` 已过时（wave941 删 Makefile）；冷启权威＝`./xbuild bootstrap-driver-seed` 两段式（xbuild all 不自足）；skill true-cold-test.md Ubuntu 序列已修。**发现三**＝Darwin seed phase1 纯 ld U `_xlang_sys_write`（rt_entry.x Cap 9.1.8 extern leaf；freestanding_io Linux-only）→ `f635c82dd` 补 `runtime_asm_io_stubs.o` 入 mk Darwin MAIN_LINK_O（与 g05_relink_env.sh 同款槽位；mac 冷启通）。**mac 真冷 12 探针 7/12**（flooronly/exact7v/iso1/fminonly/fminneg2 红；同二进制稳定、跨编译翻转）＝**arm64 f64 非确定错既有卡**（wipe 揭出、非本波引入），mac L4 不宣称。 |
| 2026-09-08 | **fix(selfhost)** **9.2.4 Fix F — fk9 数学按需门表补全 29→60 面（`f27479a03`）** | **代码＋测试** · **不升钉** · **根因**＝`labi_std_fk_gate_sym_count(fk==9)` 仅 29 needles，runtime_math_libm 34 freestanding 面缺 31（math_acos_c…math_special_near）；sole-caller TU（如仅调 math_fmin_c）永不开 fk9 门→std/math/math.o plan 叶闭→runtime_math_libm.o 不入链→ld UNDEF。**实证**＝flooronly（floor_c 在表）绿 vs fminonly（fmin_c 缺表）红：同对象同 plan 叶，唯门表异。**根修（G.4/G.7）**＝`.x` 权威 `labi_ondemand_heavy.x` 表 29..59 追加＋seed 双生 `labi_ondemand_list.from_x.c` 同序同 commit；count 29→60；双表 60/60 同步验证。**验收**＝mac force-seed 重建后 12 探针矩阵 fminonly/fminneg2/iso1 转绿＋L2 rv42/hello/ondemand 绿；后 Ubuntu L4 真冷 12/12。**附带勘误**＝探针 fminneg2 期望常量误写 0x4010…（=4.0 非 2.5；0x4004…=4612811918334230528）——探针错编译器对；seed fk7=4 vs `.x` fk7=10 漂移记 surface-mirror 既有卡（本波不动）。 |
| 2026-09-08 | **fix(selfhost)** **9.2.4 Fix E — `.x` SysV 调 xmm 重载宽度 f64→movq（`bd6745924`）** | **代码＋测试** · **不升钉** · **根因**＝x86_64 SysV spill/reload 两发射器（`glue_emit_call_args_elf_sysv_f32_xmm_c`＋`pipeline_asm_emit_call_args_elf_c` ta==0）对所有 xmm 类实参无条件 32 位 `movd` 重载，截断 f64 高 32 位（NaN 0x7ff8…→0.0）；seed `_impl` 早已条件式，产品用 `.x` 面故 Ubuntu 独红（mac/arm64 不进此两面）。**根修**＝两面补 is_f64 数组＋movq/movd 条件（复用 Fix D 分类器 `glue_arg_ref_is_f64_width_c`；连同 method-call 面共 5 处 helper 调用点全条件化）。**验收**＝mac 重建 8 探针＋L2 绿；Ubuntu pull 重建后 fpnan64/exact7v/nanmini 转绿；objdump 实证调点 movq。**教训**＝插桩 seed `_impl` 零输出→产品发射权在 `.x` 面（thin hybrid），根因定位先辨权下面。 |
| 2026-09-08 | **feat/fix(selfhost)** **9.2.4 探针链 Fix A–D＋exact-7（`c62dc219a`／`40e50d234`／`a7d76a610`／`4f49125ea`／`7a8322ba9`）** | **代码＋测试** · **不升钉** · 8 探针 NaN/f64 矩阵驱动的 backend 六连债前四刀：**Fix A**＝arm64 负浮点常量 4 处十进制误抄根修；**Fix B**＝SHARED f64→f32 demote（let-init＋assign）；**exact-7**＝floor/ceil/trunc/round/fabs/fmin/fmax 纯 `.x` 位级实现（freestanding 去 libm；fmin/fmax 零对 pin glibc 第二操作数约定；`a != a` NaN 检测双符号无关）；**Fix C**＝fp 比较无序（NaN）语义双端（x86_64 setnp/setp＋arm64 fp_inv_cond）；**Fix D**＝未盖章 f64 实参 32 位装载（movd）x86_64 根修＋分类器 ctx+fallback。**方法卡**＝IEEE 默认 NaN 符号实现定义（x86 divsd→-NaN 0xFFF8…；arm64 fdiv→+NaN 0x7FF8…），探针必须双收；十进制↔hex 常量一律 Python 验算（人工算错三次教训）。 |
| 2026-09-08 | **docs(selfhost)** **日切：7 日归档／8 号主文档重写** | **文档** · **不升钉** · 7 日 **21** 行 §6 移入 [自举进度-归档-2026-09-07.md](自举进度-归档-2026-09-07.md)（9.7.1 FILE\*→fd＋9.5.3 slice2/3a/3b＋9.7.2–9.7.7＋9.6.0–9.6.3＋9.4.2/9.4.3＋9.5.4/9.5.5＋g05/minos 运维两刀）；主文档重写：本波状态／下波候选刷 `c5b2178a0`（9.3 域收官），前排换 9.2.4 立波＋立卡，§6 只留 9 月 8 日行；双端 L2＋12 门补跑落 `/tmp/xlang_l2_Darwin_c5b2178a0.log`＋`/tmp/xlang_l2_Linux_c5b2178a0.log`。钉盘仍 `b5be5ed97`。 |
| 2026-09-08 | **feat(selfhost)** **9.3.3 裸十进制 `#if` 字面量根修（`c5b2178a0`）＋ 9.3 全节勘正收口＝9.3 域收官刀** | **代码＋测试** · **不升钉** · 日常闸门＝SHARED 双端 L2 产品矩阵。**建图**＝9.3 五条逐项勘正：9.3.1 宏重命名机制已落地（ed25519 glue；9.2.3 并条）、9.3.2 zlib `#undef` 桥已立、9.3.4 stdatomic＝语言极限有意 C 桥（standing）、9.3.5 SIMD intrinsics 桥已立；唯 9.3.3 真语义洞＝`preprocess_eval_condition_c` 单 token 无条件落 `-D` 查表，`#if 1`/`#if 0` 数字 token 恒假。**根修（G.7）**＝`runtime_pipeline_abi.x` 纯权威单 token 先纯十进制判定（非零真/`"0"` 假）；weak 冷孪生＋surface 镜像同 commit 同语义。**验收双端**：.o 直连 probe 5/5（lit1=1/lit0=0/lit2=1/FOO=0/empty=0）＋`run-preprocess.sh` run=16（新增 `if_numeric{1,0}.x`）＋勘正门证据（f04-crypto-closure／f04-zlib／chacha 双端 ok；f-atomic Ubuntu ok；simd-s1 双端 ok）＋双端 L2 **5/5**＠`c5b2178a0`＋**12 门全 ok**。**运维发现**：mega `.x` 体内改动须 `XLANG_HOST_CC_SEED_FORCE=1`（平日 inject-only thin 捷径不吃 mega 体）。下一刀：9.2.4 libm fdlibm 级大域立波。 |
| 2026-09-08 | **feat(selfhost)** **9.2.1 mbedtls BIO send/recv — Cap net 面闭环（`2a9f3dcae`）＝9.2 首刀** | **代码＋测试** · **不升钉** · 日常闸门＝SHARED 双端 L2 产品矩阵。**建图**＝9.2 全节初勘：zlib＝extern 系统 -lz（宏桥＝9.3.2 域）、libm＝fdlibm 级、ed25519＝.inc 宏域、sqlite/arrow＝extern 系统库大域——唯 9.2.1 是真 Cap 残（BIO 回调 libc `send`/`recv`）。**根修（G.7）**＝`runtime_tls_mbedtls_bio` 回调改 `xlang_net_sendto`/`xlang_net_recvfrom`（9.1.7 权威；flags 0＋NULL 地址字节语义等价；Cap「负返回→errno」映射保留 EAGAIN 分支）；mbedtls 本体仍系统库（依赖有意保留）。**验收双端**：try-heat 重建 rc=0＋nm U 面 send/recv 清零（余 errno 脸＋mbedtls_ssl_set_bio）＋socketpair probe 双端 3/3（roundtrip r=5／EAGAIN→WANT_READ=-26880／写满→WANT_WRITE）＋双端 L2 **5/5**＠`2a9f3dcae`＋**12 门全 ok**。下一刀：9.2.2–6／9.3 逐条勘正立波。 |
