# Xlang / X language 自举进度

> **更新**：2026-09-10 · tip **`a58899348`**（docs）· 前代码 **`93935df52`** · v3 尝试波已回退；**冷重建画布已就绪**（rebuild-only 绿＠`a58899348`：矩阵 5/5＋drift-gate 15/5/0＋m2/m6=42） · **钉盘 `fa1061224`**（2026-09-10 升钉 · 双端 L4 真冷全绿＋bstrict 129/129 · Ubuntu 15m18s／Darwin 21m50s · §3.3 run-compress 冷链红已闭）· 前 `e194cd294`（09-09 晚）／`d583f61f5`／`b5be5ed97` · **金标 SSH＝`ubuntu-server`** · Ubuntu 验＝**git pull（禁 scp）** · Ubuntu L2 **钉盘节奏**\
> **产品 L4 放行钉盘**：**`fa1061224`**（2026-09-10 升钉 · 双端 L4 真冷全绿＋bstrict 129/129；§3.3 compress 车辆前缀注入）· 前 **`e194cd294`**（09-09 晚；Darwin `O_DIRECTORY`）· 前 `d583f61f5`（晨；Darwin 侧曾把 bstrict 管道进 tail -3 假绿）· 再前 `b5be5ed97` 2026-09-02 · `e8176cbe5` 2026-08-23 \
> **产品 residual tip**：link_abi **7.3.1 关 pin ✅**（12 labi_*.x 切片 prefer；默认 FROM_X=1）· parser **7.2.2 关 pin ✅**（tip `-E` + assemble；产品默认 FROM_X=1）· codegen 7.4.2 wave323 ✅ · typeck 7.4.1 wave322 ✅ · runtime monofile wave321 ✅ · present **0** · 钉盘 **`b5be5ed97`** · Stage 8 tip **wave332 Track L 30/30 FULLY CLOSED**（**PRODUCT HALF=0** · **PRODUCT PINNED=0** · **PRODUCT RETIRED 23/23 = 100%**）· **7.4.3 PREFER_X_O 双端 8/8 wave338 ✅** · **prefer 族 pure-asm 产品默认 ✅** · **pipeline_abi pure-asm 硬禁**\
> **tip L4 安全网（历史）**：**`ffc0b646f`**（已由 tip L4 升钉吸收；前网 `4bacaf18c`）\
> **终局债**：[C迁移追踪.md](C迁移追踪.md)（状态 only · 无波次流水）· [Makefile迁移表.md](Makefile迁移表.md)\
> **本波状态**（一行）：本波主刀＝**§3.3 run-compress 冷链红根修＠`fa1061224`**。旧产品（e194cd294 worktree）A/B 定罪：旧 mod.x `-E` 仍 co-emit 限定 import dep 前缀体（`std_compress_gzip_libz_*`），`7e2fef61b`（skip std/ in need_coemit）停掉 co-emit 后，车辆 impl 发射两 lane 均裸名→U 前缀/T 裸错配。修＝G.7 补全车辆自有契约（「impl 无 -lib-name → 路径前缀」）：std/compress/ 下 impl 源（非 mod.x）的 `^export function` 名单在生成 C 上文本前缀化（名单动态取自源文件，无第二套符号表；FFI extern "C" 不动）。**双端 L4 真冷全绿＋bstrict 129/129 → 升钉 `e194cd294`→`fa1061224`**。
> **下波候选**：①MSVC va／seed WIN leftover addr_of（须切 Windows，VM 当前离线）；②7.4.4 v3（`-E-extern` 缺口尝试波已回退；重试姿势＝先全量冷消航道混再单变量，见 C迁移追踪 7.4.4 知识卡）；③7.2.1 parser pinned seed 退役＝多波史诗（seed 4,555 行＋parser_asm 切片族；pthin 源仅 237 行）。日常闸门＝**双端 L2 产品矩阵**＋**升钉前 `./xbuild drift-gate`**。

> **效率纪律（2026-08-07）**：禁止再开「一 BSS cell 一波」；一波至少一 **域文件** 真减 host-cc LOC 或 present−1；过程上 prefer+heat 一次／域，双端矩阵同波一次\
> **验证纪律**：**自举期暂停 check 闸门**（L2/L4 均不跑 `xlang check`，2026-08-05）；**日常闸门＝SHARED 双端 L2 产品矩阵**（mac + Ubuntu `./xbuild l2-matrix`；wave299：捕获 rv=42／opt=102 成功码，禁 set -e 截断）。leftover-PE unique（`!FROM_X` 或 `WIN_LEFTOVER`）不进 leftover rest FROM_X 产品；leftover-PE 探针红 **不**当双端 L2 产品红。谈自举／升钉才 L4 + 矩阵 + 全量 bstrict；bstrict **per-script timeout** Ubuntu 300s／Darwin L4 1200s + catalog cache；**墙钟**：`product_l4` 分步 wall + `run-all-bstrict`（`XLANG_BSTRICT_TIMING_FILE`）；并行 **`XLANG_BSTRICT_JOBS` 默认 1**（Ubuntu 金标 1；Darwin L4 默认 1／GATE_CASE_JOBS=1；JOBS=8／4／2 本机 OOM）

> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景（换 IDE 续作）**：[自举时序.md](自举时序.md)（S0–S8 先→后 · 30 秒协议）\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[08](自举进度-归档-2026-09-08.md) · [07](自举进度-归档-2026-09-07.md) · [06](自举进度-归档-2026-09-06.md) · [05](自举进度-归档-2026-09-05.md) · [04](自举进度-归档-2026-09-04.md) · [03](自举进度-归档-2026-09-03.md) · [02](自举进度-归档-2026-09-02.md) · [01](自举进度-归档-2026-09-01.md) · [31](自举进度-归档-2026-08-31.md) · [30](自举进度-归档-2026-08-30.md) · [29](自举进度-归档-2026-08-29.md) · [28](自举进度-归档-2026-08-28.md) · [27](自举进度-归档-2026-08-27.md) · [26](自举进度-归档-2026-08-26.md) · [25](自举进度-归档-2026-08-25.md) · [24](自举进度-归档-2026-08-24.md) · [23](自举进度-归档-2026-08-23.md) · [22](自举进度-归档-2026-08-22.md) · [21](自举进度-归档-2026-08-21.md) · [20](自举进度-归档-2026-08-20.md) · [19](自举进度-归档-2026-08-19.md) · [18](自举进度-归档-2026-08-18.md) · [17](自举进度-归档-2026-08-17.md) · [16](自举进度-归档-2026-08-16.md) · [15](自举进度-归档-2026-08-15.md) · [14](自举进度-归档-2026-08-14.md) · [13](自举进度-归档-2026-08-13.md) · [12](自举进度-归档-2026-08-12.md) · [11](自举进度-归档-2026-08-11.md) · [09](自举进度-归档-2026-08-09.md) · [08](自举进度-归档-2026-08-08.md) · [07](自举进度-归档-2026-08-07.md) · [06](自举进度-归档-2026-08-06.md) · [05](自举进度-归档-2026-08-05.md) · [04](自举进度-归档-2026-08-04.md) · [03](自举进度-归档-2026-08-03.md) · [02](自举进度-归档-2026-08-02.md) · [01](自举进度-归档-2026-08-01.md) · …\

### 维护约定

1. 每完成一波 **必改**本文（KPI / 前排 ≤3 / **§6 一行**）；动终局债时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序 · **一行一波** · 描述宜短：做了什么 · tip · 不升钉 · 下一刀）。**禁止**在 §0–§5 堆 `waveNNN` 长清单；细节进 LEAF / C迁移 / 当日归档。
   - **§6 表固定 3 列**：`日期 | 波次 | 描述`。描述列**禁止**裸 ASCII `|`（会把行拆成 4+ 列）；并列名写 `／`、`或`、全角 `｜`，或改写避免。
3. **禁止**把 prove / Stage2 / N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**）。
5. 工程 KPI 写 **SHA + 日志路径**；过期整日流水 **日切归档**后主文档重新起表（见归档头）。
6. 大债地图写 [`当前问题分析.md`](当前问题分析.md)；非每微步。
7. **`当前进度.md` 已删除**（2026-07-30；与本文重复；勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 = `fa1061224`**（2026-09-10 升钉：双端 L4 真冷＋bstrict 129/129 · §3.3 compress 车辆前缀注入 · 前 `e194cd294` 09-09 晚；再前 `d583f61f5`／`b5be5ed97`）。\
**MG 终局达成 = wave941/942**（Makefile 物理删除 · 0 make in bootstrap · catalog 单权威 · 双端 L4 绿）\
**双端 Stage2 = ✅ SHA256 match**（Ubuntu `9e60e1290` · macOS `343894709` · `xlang_asm_stage1`／`xlang_asm2` bit-identical）\
**选题权威 =** [C迁移追踪.md](C迁移追踪.md) DAG（**变更记录只在本文 §6**）。\
**本波主刀 = parse 层悬空值吞噬守卫＠`45f8296a5`（钉盘仍 `e194cd294`）**。9.4.3 v1 flag 值表＠`1c719831f`。fnptr-as thin 委托＠`392d44cb6`。scalar ADDR_OF／fn-ptr 全局格＠`94b9ba071`。9.4.2 ADDR_OF **表**＠`0a63ef5e7`。parser slen>127＠`9bc09b934`。p3 家族＠`ab1c1fbf8`。Defect D＠`76842942c`。RELA＋字串池＠`1fdec53e8`。`x_stubs` 前缀 ast -1＠`0133f91a0`。strict_glue／`x_stubs` 前缀 elf_o -1＠`62af44889`。postlink compiler-fallback 禁静默拷 `./xlang`＠`b337b66b5`。Darwin keep／smoke／postlink 禁静默拷 experimental＠`6449fa44d`。退役 mega leftover sibling `_impl` 包装＠`79bc22058`。退役 mega leftover via_cparser `_impl` 包装＠`62b4dd253`。退役 leftover `-E-extern` cparser 调用点＠`16dd9b577`。退役 leftover `shu_c_sibling` 调用点＠`0c400d576`。退役 leftover 泛型 lexer／parse 块＠`4c0ec6afe`。退役 `rt_run_*` leftover `!XLANG_NO_C_FRONTEND` 调用点＠`a08d04d70`。退役 mega stale `c_frontend_smoke`／`check_only_c_typeck`＠`36bb731f1`。退役 mega stale `smoke_lex_dump`／`c_typeck_entry_*`＠`023b26d09`。`asm_codegen_elf_o` 5 参 -1 leftover＠`81b2a6f98`。退役 mega stale esc_gate＠`9ae7d8349`。bridge 4 参 weak 桩先序＠`2eb6aaa83`。modlet 表 64 项上限＠`213ca9ec5`。experimental promote 链＠`ddf74296e`。Ubuntu WPO `main.o` CG002＠`564278d29`。Darwin wpo-full-chain ensure＠`6e4478892`。va_arg 单实例＠`4ca63e4cd`。va gate＠`f35b03fe1`。pabi surface regen＠`f8156c1c6`。compress stream＠`661f2970c`。c_face 转发＠`7e2fef61b`。u64 setcc＠`1adfbaa58`。立卡仍：MSVC va／seed WIN leftover addr_of（均须切 Windows）。PREFIX elf_o／ast -1 leftover 全闭。RELA＋字串池已闭。Defect D 已闭。p3 家族已闭。parser slen>127 L011 已闭。9.4.2 ADDR_OF／fn-ptr **表** bake 已闭。scalar ADDR_OF／fn-ptr 全局格已闭。fnptr-as thin 内联 spell 已闭。9.4.3 v1 flag 值表已闭。**parse 层悬空值吞噬已闭**。

| 指标 | 状态 |
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`e194cd294`**；residual tip present **0**；Stage 8 **30/30 FULLY CLOSED**；PRODUCT RETIRED 23/23 |
| Track MG | **11.3.1 MG ✅** |
| Track BC | **🟢 Stage8 30/30** · M4 **5/5** · present **0**；`pipeline_x` mega 仍 host-cc |
| Track L | **收 30/30 FULLY CLOSED** |
| mac / Ubuntu L4（钉盘） | ✅ 双端 L4 + bstrict **129/129** @ **`e194cd294`** |
| pin 蛋 | ✅ darwin.arm64 + linux.x86_64（本波 `xlang_asm`） |
| 本波构建级别 | **tip 双端 L4（红在新卡）**：首跑双端 phase1 红（缺 mangled 桥面）→根修＠`7f62aa9da`→重跑双端 **过 phase1／bootstrap（Ubuntu 329s／Darwin 199s）／g05／矩阵 5/5**，bstrict 双端红＝run-compress（§3.3 新卡，唯一红）；其余 128 脚本双端绿。**不升钉**（钉盘仍 `e194cd294`）。check 闸门暂停 |

***

## 1. 文档地图

| 文档 | 看什么 |
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [自举时序.md](自举时序.md) | **先→后全景** · 换 IDE 30 秒协议 · S0–S8 主航道 |
| [C迁移追踪.md](C迁移追踪.md) | **终局债 / BC·PC DAG**（状态 only · 无波次流水） |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild 叶映射（MG 已 ✅） |
| [compiler/docs/LEAF_PATTERN_RESIDUAL.md](../compiler/docs/LEAF_PATTERN_RESIDUAL.md) | 叶 residual 人读地图 |
| **本文** | 长期 KPI + 前排 ≤3 + **§6 波次一行记录**（**9 月 9 日**；8 日见 [归档](自举进度-归档-2026-09-08.md)） |
| [自举进度-归档-2026-09-08.md](自举进度-归档-2026-09-08.md) | **9月8日** 9.3 域收官＋9.2 全域（fdlibm libm 大域/sqlite/zlib/gzip/8B ABI/f64 AAPCS64）＋std 红簇 12 项全闭（env→clz→布局三面→DEREF→盖章→block_ref→PTR 剥壳＝run-string 绿）＋闭图 §3.1–3.3（**44** 行） |
| [自举进度-归档-2026-09-07.md](自举进度-归档-2026-09-07.md) | **9月7日** 9.7.1 FILE\*→fd 统一＋9.5.3 slice2/3a/3b＋9.7.2–9.7.7 系列＋9.6.0–9.6.3 根修＋9.4.2/9.4.3 双端闭环＋9.5.4/9.5.5＋批量收编 docs 行（**21** 行） |
| [自举进度-归档-2026-09-06.md](自举进度-归档-2026-09-06.md) | **9月6日** Cap 9.1/9.4/9.5 全平台闭环＋批量收编 `461834bd1`＋10.6.x Darwin thread/sync/fnptr＋9.5.3 slice3a/b＋Windows MATCH arm BLOCK 前序（**73** 行） |
| [自举进度-归档-2026-09-05.md](自举进度-归档-2026-09-05.md) | **9月5日** leftover unique MATCH dest-parked FIELD／INDEX／DEREF／BLOCK／IF＋双端 L2＠`7290c79e0`（**64** 行） |
| [自举进度-归档-2026-09-04.md](自举进度-归档-2026-09-04.md) | **9月4日** 纯 asm 邻域→ARRAY_LIT TYPE_FN STRUCT_LIT 字段（**40** 行） |
| [自举进度-归档-2026-09-03.md](自举进度-归档-2026-09-03.md) | **9月3日** leftover unique 113→0／P2 Darwin `-o`／windows-gate 全绿（**90** 行） |
| [自举进度-归档-2026-09-02.md](自举进度-归档-2026-09-02.md) | **9月2日** 升钉 `b5be5ed97`／Darwin L4 JOBS／Windows leftover-PE hybrid unique 113→50（**52** 行） |
| [自举进度-归档-2026-09-01.md](自举进度-归档-2026-09-01.md) | **9月1日** typeck pin 7.4.6–8／L4 多轮／Cap va／stage10（**131** 行） |
| [自举进度-归档-2026-08-31.md](自举进度-归档-2026-08-31.md) | **8月31日** stage10 开刀／seed dest-SLICE／leftover／假权威诚实等（**113** 行） |
| [自举进度-归档-2026-08-30.md](自举进度-归档-2026-08-30.md) | **8月30日** leftover XLANG fallthrough F-06／F-07 等（**111** 行） |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图（本波不改） |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | 现行白名单；per-script timeout=300s + catalog cache |
| 产品 L4 放行钉盘 | **`fa1061224`** | 2026-09-10 升钉；双端 L4 真冷 + bstrict 129/129；前 `e194cd294` |
| pin 蛋 | **darwin.arm64 + linux.x86_64** | 自钉盘 L4 `xlang_asm` 刷新 |
| residual tip（BC） | present **0** | Stage 8 30/30 · M4 5/5 · prefer pure-asm 默认 · pipeline_abi mega **硬禁** |
| mac 钉盘 L4 | ✅ L4 + bstrict **129/129** · 21m50s 全程 | `@fa1061224` `/tmp/xlang_l4_Darwin_fa1061224.log` |
| Ubuntu 钉盘 L4（金标） | ✅ L4 + bstrict **129/129** · 15m18s 全程 | `@fa1061224` `ubuntu-server` `/tmp/xlang_l4_Linux_fa1061224.log` |
| 产品矩阵（日常 L2） | ✅ **`./xbuild l2-matrix`** | 双端 5/5＠`45f8296a5`（parse 悬空值守卫；rv=42／opt=102；g05 driver_x.o＋driver_compile_x.o＋runtime_driver_no_c.o 双端重建） |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 结案 |
| **N** | 111/111 IDENTICAL | 本波不变 |
| **Stage2** | ✅ 双端 tip SHA256 STRICT match | 钉盘历史见归档 |
| **R2 / Cap** | 按需 | 明细见 29／31／1 日归档 |

### 2.3 删 Makefile 闸门（诚实 · MG 已结）

| 键 | 值 |
|----|-----|
| `ENDGAME_PHYSICAL_DELETE_MAKEFILE` | **1** |
| `TREE_ARMED` / `BODY_SHIPPED` / `DELETE_ALLOWED` | **1** / **1** / **1** |
| post-delete residual | **✅** wave944–964 |

***

## 3. 当前前排（≤3 条）

1. **下一刀** — **§3.3 run-compress 冷链红根修**（双端 L4 bstrict 同源红：std/compress 子模块 .o 的 lib 面裸名 vs mod.x 前缀调用错配；修后重跑双端 L4 全绿→升钉）。link_alias mangled 桥面已闭＠`7f62aa9da`（phase1 卡）；parse 层悬空值吞噬已闭＠`45f8296a5`；9.4.3 v1 flag 值表已闭＠`1c719831f`；fnptr-as thin 已闭＠`392d44cb6`；scalar ADDR_OF 格已闭＠`94b9ba071`；9.4.2 表 bake 已闭＠`0a63ef5e7`。
2. **立卡（Windows）** — **10.7.1 MSVC va**（POSIX host-C 同符号已闭；MSVC 须切 Windows）＋ **seed 第二 WIN-leftover addr_of 变体**（leftover-PE unique；Ubuntu 在线不挑）。
3. **立卡不动（前端/产品面陈债）** — spawn 真体 `driver_try_compile_via_shu_c_sibling` 仍 T（有意保留；mega `_impl` 包装已闭＠`79bc22058`）；nest 冻帽 64。钉盘 **`e194cd294`**。PREFIX elf_o／ast -1 leftover 全闭。RELA＋字串池已闭。Defect D 已闭。p3 家族已闭。parser slen>127 L011 已闭。9.4.2 表 bake 已闭。scalar ADDR_OF 格已闭。fnptr-as thin 已闭。9.4.3 v1 flag 值表已闭。parse 层悬空值吞噬已闭。

***

### 3.1 建图：run-compress＝asm 直链航道的 zstd/brotli 第三方库接入（✅ 已闭＠`d583f61f5`）

- **现状**：g15 针表已扩（stream_state_bytes 等 4 针）且 gdb 实证触发；UNDEF 残＝`std/compress/{zstd,brotli}` 子模块 `lib.x` 的 extern C FFI（`ZSTD_*`／`BrotliEncoder*/Decoder*`）。
- **`-l` 旗标面缺口**：`-lzstd`／`-lbrotlienc/-lbrotlidec` 只在 C 前端车道推送（`labi_invoke_cc_list.x` generated-C 针）；run-compress 走 asm `-o` 直链（raw `ld.bfd` 对象清单，无 cc 旗标）。
- **zlib 先例**：`std/compress/zlib/libz.x`＝X 移植（自带 zlib.o，零系统依赖）——zstd/brotli 若循此路＝大移植工程（9.2 域「第三方」）。
- **替代**：asm-lane 需要时推系统 `.so` 绝对路径（Ubuntu 有 libzstd.so/libbrotlienc.so/libbrotlidec.so；raw ld 可直接链 .so）——须过零 libc／静态产品策略与 macOS 双端（brew 路径 `ld_append_brew_lib_paths` 已有先例）。
- **回归集**：run-compress＋zlib/gzip 邻域（run-gzip 类）＋L2。
- **2026-09-09 实施受阻记录（诚实·两轮）**：sg15 块补推 zlib.o/gzip.o＋系统 `.so` 后 **labi 多切片 prefer 断链**：`runtime_link_abi.o` 丢 `xlang_asm_ld_append_on_demand_user_objs` 定义。第二轮取证（仪器化 ensure 保留中间产物）：
  - pure-asm 切片对 heavy.x **恒失败**（既有：fn #139 `lambi_od_push_test_monofile_companions` T001「extern call requires unsafe」＋XT001）→ 常态走 `-E`+cc 后备，与编辑无关；
  - 基线合并件 542 T／defs=1；编辑件 552 T（+10，我的新符号在）／defs=0——**三块中间件（L8b/L8c/rest）在基线与编辑两轮都 0 defs**，定义者另有来源（疑 L0..L9 某 mktemp 切片或合并阶段行为），未及捕获即耗尽预算；
  - 字面量直传实参非元凶（改 let-local 安全写法后 defs 仍 0）。
  已完整回退保双绿；下一刀：给 L0..L9 mktemp 切片也加保留，找出基线定义者真身，再落 sg15 推送。
- **✅ 谜底（第三轮仪器化，已解）**：
  - **基线定义者＝L8b full-seed 后备件**（`seeds/labi_ondemand_list.from_x.c` 的 cc 编译）——基线 `labi_ondemand_heavy.x` **-E 恒失败**（fn #142 `xlang_asm_ld_append_on_demand_user_objs` 自身**既有 typeck 债**：check_block failed）→ l8c_x_ok=0 → **split 分支永不走**、L8b 落 full-seed（含定义者）＝**活航道纯属意外兜底**；
  - 我的 sg15 编辑（+30 行，在该函数体内）让 heavy.x **-E 变干净**→翻转进 split 航道→而 **-E C 后端不发射该函数**（824KB 输出 grep=0，真发射缺口）→ UNDEF。即：**任何让 heavy.x -E 变干净的编辑都会引爆此雷**；
  - **seed-only 航道试验**（只改 seed 伴块＋撤 heavy.x）：defs=1✓ 但 run-compress 仍 UNDEF——门禁走 **`xlang build` 子命令**（build.x 编排），其链路**不经过** `xlang_asm_ld_append_on_demand_user_objs` 的 sg15 伴块推送面（或 push_obj 不收绝对 .so）＝**第二缺口：build 子命令链路的 on-demand 推送面另有其人**。
  - 下一刀改道：①追 `xlang build` 子命令（build.x）的 on-demand push 面（谁在 build 路径推 std/compress/*.o）；②heavy.x fn #142 既有 typeck 债＋-E 发射缺口立卡（动它前须先补 -E 发射，否则清 typeck 反而引爆 split 雷）。
  - **✅ 已闭＠`189eb9b08`**：活链＝`xlang build`→asm ld→`xlang_asm_ld_append_on_demand_user_objs`（L8c heavy.x）；假绿＝c_face `return -1`＋sg15 不 push gzip.o＋`compress_o=NULL`。修＝c_face 转发＋sg15 伴推＋facade 针。Darwin gzip 真往返。残＝zstd/brotli catalog。
  - **✅ 已闭＠`e0b5013b4`**：zstd/brotli 循 gzip 同路。catalog `zstd.o`／`brotli.o`（mod+lib）；c_face 转发 `std_compress_{zstd_zstd,brotli_brotli}_*`；sg15 伴推；L8b/seed facade 针 2→10 开火 `-lzstd`／`-lbrotli*`。禁 Linux-only 绝对 `.so` 进 heavy.x。双端 zs/br probe=0。残＝stream init/process/end 仍 `-1`。

***

### 3.2 L8b 航道非确定性＝g15 表 24 vs 28（✅ 已闭＠`51327d07b`）

- **真凶**：`.x` g15＝28，full-seed `labi_od_simple_group_sym_count(g==15)` 仍 24。L8c prefer 常败 → seed first-wins；针 i==24..27 已在 seed 却因 count=24 永不走。`grep return 28` 命中 g9 slice 当「seed 已 28」。
- **第二刀**：针触发后 Ubuntu 仍 UNDEF＝c_face 缺 4 T（mac dead_strip 藏）；`.so` 曾误嵌 `core/slice/mod.o` 块。
- **修**：`e6c8d28f2` count 24→28；`51327d07b` c_face 4 T＋`.so` 归位。heavy.x 不动。
- **验**：双端 L2 5/5＋run-compress 绿；Ubuntu `cmpl $0xf`→`mov $0x1c`。未 L4。

***

### 3.3 run-compress 冷链红＝std/compress 子模块 lib 面裸名 vs mod.x 前缀调用错配（✅ 已闭＠`fa1061224` · 2026-09-10 双端 L4 全绿升钉）

- **症状**：双端 L4 bstrict 唯一红＝`run-compress.sh`；ld UNDEF `std_compress_{gzip_libz,gzip? ,zstd_lib,brotli_lib}_compress_*_c`（含 8 件 stream/one-shot 全套），引用者＝`std/compress/{gzip,zstd,brotli}/*.o` 的 mod 面（如 `std_compress_gzip_gzip_compress`）。温态重跑同样红（非冷专属）。
- **取证链（已钉死）**：①Ubuntu 现场 `std/compress/gzip/gzip.o` 对 lib 面 **T=0／U=8**——mod+lib 合并发射丢 lib 体；②vehicle（`xlang_compile_std_module.sh`，spec `mod|1|mod.x|libz.x`＝BARE_IMPL）的 libz.x 编译：`-backend c -o` 先被 `invoke_cc_host_cc_spawn_gate` 默认拒（`host-cc-requires-allow`，Stage 12.2.3 策略），`|| true` 容忍后走 KEEP_C 恢复——`mod_1.c` 存在且手动 `cc -c` 通过，但符号是**裸名** `compress_gzip_compress_c`；③mod.x 侧 `libz.compress_gzip_compress_c`（限定调用）发射为**路径前缀** `std_compress_gzip_libz_compress_gzip_compress_c` → 错配 UNDEF；④分岔点＝vehicle 的 `LIB_NAME_SUPPORTED` 探针（`-x -E -lib-name ""`）：当前产品＝**支持**（→`-lib-name ""`→裸名）；昨日 L4（`e194cd294`）绿说明当时该探针走的是另一条路（未支持→路径前缀→匹配）。
- **二次深挖（2026-09-10 凌晨，排除面再收窄）**：⑤compress 面（vehicle／`std/compress/`／run-compress.sh）在 `e194cd294..tip` **零改动**——回归在编译器发射侧；⑥`-x -E`（非 `-o`）lane 对 libz.x **也裸名**（两 lane 同病）；改名文件／换路径均不变——**非文件名判定**；⑦`xlang_entry_lib_name_from_path`（权威在 runtime_pipeline_abi.x）窗口内未动；⑧vehicle 文档契约自述「impl 无 `-lib-name` → 路径前缀」，与今日实测（裸）矛盾——**impl 前缀推导在当前产品上退化**；⑨对照：heap（mod|0 多文件）今日 impl 生成 C 同样裸名但最终 .o 得前缀 T（`std_heap_libc_heap_alloc_c`）——存在某个「合并期裸→前缀」步骤对 heap 生效、对 compress 不生效（最终 rename 权威明文 **never wrap `*_c`**（@71d9c714e 回归教训），compress lib 面全是 `*_c` → 永不改名）；⑩Darwin nm 前导下划线坑：`T _std_…` 需带 `_` grep（本波早期 T=0 误判源于此）。
- **下一步实验（定罪用）**：worktree 检出 `e194cd294` 一次性 g05 造旧产品 → 对 `std/compress/gzip/libz.x` 跑同款 `-backend c -x -E -L ..` 对照 C 前缀（昨日应前缀、今日裸）→ 二分窗口内波次；随后按真凶层修（发射层恢复 impl 路径前缀，或 vehicle 层统一 compress 族绑定面——**禁**在 mod.x 改裸 extern 去适应退化（那会在发射修复后再次错配）。
- **修法方向（下波定）**：A＝vehicle 对 compress 族（mod.x 用限定调用）不做 bare（spec `mod|1`→`mod|0` 或按 mod.x 是否限定调用自适应）；B＝import 限定调用的 mangling 与 `-lib-name ""` 裸发射统一（前端层，爆炸半径大）；倾向 A（vehicle 层、单权威、最小）。禁在 ld 尾部加 alias 糊。
- **复现**：`rm std/compress/gzip/gzip.o && (cd compiler && bash scripts/xlang_compile_std_module.sh ensure ../std/compress/gzip/gzip.o)` → `nm … | grep -c "T std_compress_gzip_libz"`＝0；随后 `bash tests/run-compress.sh` UNDEF 同名。
- **关联已闭**：§3.1 zstd/brotli catalog＋伴推（`e0b5013b4`）／§3.2 L8b 非确定性（`51327d07b`）——本卡在其后、独立（vehicle 发射层，非链路推送层）。

***

## 4. 与归档的关系

- **9月8日归档**：[自举进度-归档-2026-09-08.md](自举进度-归档-2026-09-08.md) — **44** 行 · 9.3 域收官＋9.2 全域（fdlibm libm/sqlite/zlib/gzip/8B ABI/f64 AAPCS64）＋std 红簇 12 项全闭 · 钉盘 **`b5be5ed97`**。
- **9月7日归档**：[自举进度-归档-2026-09-07.md](自举进度-归档-2026-09-07.md) — **21** 行 · 9.7.1 FILE\*→fd 统一＋9.5.3 slice2/3a/3b＋9.7.2–9.7.7＋9.6.0–9.6.3＋9.4.2/9.4.3＋9.5.4/9.5.5＋运维两刀 · 钉盘 **`b5be5ed97`**。
- **9月6日归档**：[自举进度-归档-2026-09-06.md](自举进度-归档-2026-09-06.md) — **73** 行 · Cap 9.1/9.4/9.5 全平台闭环＋批量收编 `461834bd1` · 钉盘 **`b5be5ed97`**。
- **9月5日归档**：[自举进度-归档-2026-09-05.md](自举进度-归档-2026-09-05.md) — **64** 行 · leftover unique **0** · 钉盘 **`b5be5ed97`**。
- **9月5日及前**：见 §1 归档链接行。
- **勿**用归档 tip 或旧钉冒充现行 tip L4。**现行产品钉盘 = `e194cd294`**。
- 日切后主文档只保留 KPI + 前排 + **9 月 9 日** §6；再堆满则再日切。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
| **macOS arm64** | 钉盘 L4＠**`e194cd294`** · **tip L4＠`7f62aa9da`**：phase1／bootstrap 199s／g05／矩阵 5/5 全过；bstrict 15m47s 唯一红＝run-compress（§3.3 新卡，双端同源）。日志 `/tmp/xlang_l4_Darwin_7f62aa9da.log` |
| **Ubuntu x86_64（金标）** | **tip L4＠`7f62aa9da`（git pull）**：phase1／bootstrap 329s／g05／矩阵 5/5 全过；bstrict 7m0s 唯一红＝run-compress（§3.3）。日志 `ubuntu-server:/tmp/xlang_l4_Linux_7f62aa9da.log` · SSH＝**`ubuntu-server`** |
| **Windows MSYS2** | leftover unique **0** · min-gate **全绿** · 纯 asm邻域 **6／6**＋call／两 let／赋值／while／for／break／continue／`loop`／match／数组 INDEX／STRUCT_LIT＋FIELD／裸 STRUCT_LIT.field／CALL.field `mk().x`／nested `mk().p.f`／pointer intermediate／METHOD.field `P.mk().x`／9–16B dual-GP CALL.field／sret ＞16B／`take(p: *P)`／`x as i32`／`unsafe { return *p }`／`let y: i32 = *p`／Cap-fn-ptr `foo as *u8`／TYPE_FN `function(): i32` 间接 call／参／字段／ADDR_OF INDEX／FIELD `&a[0]`／`&s.x`／TYPE_FN return `return foo`／TYPE_FN CALL of CALL `getf()()`／nested ADDR_OF `&s.a[0]`／`&a[0].x`／`&*p`／ADDR_OF CALL／STRUCT_LIT 基 `&mk().x`／`&(P { x: 3 }).x`／`&P.mk().x`／ARRAY_LIT TYPE_FN `[foo, bar]`／`fs[0]()`／STRUCT_LIT 字段 `H { fs: [foo] }`／作参 `apply([foo])`／16B `apply([foo, bar])`／TYPE_SLICE `take([foo])`／VAR packing／TYPE_FN let-init COMMON BSS／INDEX TYPE_SLICE 形参 bounds／nso_at 非 RETURN expr-stmt／INDEX TYPE_SLICE 形参／局部 store／ADDR_OF INDEX TYPE_SLICE `&xs[0]`／ADDR_OF INDEX TYPE_SLICE 非字面量 `&xs[i]`／INDEX DEREF `*[]i32` `(*p)[1]`／TYPE_SLICE DEREF 作参 `take(*p)`／CALL 返回 TYPE_SLICE 作参 `take(mk())`／FIELD TYPE_SLICE 作参 `take(s.xs)`／CALL 返回 TYPE_SLICE let-init `let s = mk()`／CALL 返回 TYPE_SLICE let-init 局部 `.length`／ARRAY_LIT TYPE_SLICE 局部 `.length`／TYPE_ARRAY 8B DEREF 作参 `take(*p)` of `*[2]i32`／FIELD TYPE_ARRAY 8B 作参 `take(s.a)`／CALL 返回 TYPE_ARRAY 8B 作参 `take(mk())`／INDEX of `[2][2]i32` 作 TYPE_ARRAY 形参 `take(rows[1])`／FIELD TYPE_ARRAY 16B 作参 `take(s.a)` of `[4]i32`／TYPE_ARRAY DEREF let-init `unsafe { let y: [2]i32 = *p }`／TYPE_ARRAY 整数组赋值 `y = *p`／TYPE_ARRAY dest-in-rbx `*p = a`／TYPE_ARRAY 整数组 return `return a`／TYPE_NAMED dest-in-rbx STRUCT_LIT `*p = P { x: 3, y: 4 }`／TYPE_SLICE dest-in-rbx ARRAY_LIT `*p = [3, 4]`／TYPE_SLICE dest-in-rbx FIELD `*p = s.xs`／TYPE_SLICE dest-in-rbx INDEX `*p = rows[1]`／TYPE_SLICE VAR dest FIELD `b = s.xs`／TYPE_SLICE let-init INDEX `let s = rows[1]`／ADDR_OF INDEX of `[2][]i32` `&rows[1]`／TYPE_SLICE DEREF let-init `unsafe { let s: []i32 = *p }`／TYPE_SLICE VAR let-init `let s: []i32 = xs`／TYPE_SLICE dest-in-rbx VAR `*p = xs`／TYPE_NAMED dest-in-rbx STRUCT_LIT `(*p).y`／**TYPE_NAMED dest-in-rbx MATCH 8B**／**TYPE_NAMED dest-in-rbx MATCH 16B**／**TYPE_NAMED dest-in-rbx MATCH 24B**／**TYPE_NAMED dest-in-rbx nested STRUCT_LIT**／**TYPE_NAMED dest-in-rbx MATCH arm VAR**／**TYPE_NAMED dest-in-rbx MATCH arm CALL**／**TYPE_NAMED dest-in-rbx MATCH arm CALL 24B**／**TYPE_NAMED dest-in-rbx MATCH arm CALL 32B**／**TYPE_NAMED dest-in-rbx CALL**／**TYPE_NAMED dest-in-rbx METHOD**／**TYPE_NAMED dest-in-rbx MATCH arm METHOD**／**TYPE_ARRAY dest-in-rbx MATCH**／**CALL.field 32B** `mk().h`／**VAR dest MATCH arm CALL** `d = match { 1 => mk(); }`／**TYPE_ARRAY dest-in-rbx MATCH arm CALL** `*p = match { 1 => mk(); }`／**TYPE_SLICE dest-in-rbx MATCH** `*p = match { 1 => [3, 4]; }`／**TYPE_SLICE VAR dest MATCH** `d = match { 1 => [3, 4]; }`／**MATCH arm nested MATCH dest-parked**／**MATCH arm STRING_LIT dest-parked TYPE_SLICE**／**MATCH arm STRING_LIT dest-parked TYPE_ARRAY**／**TYPE_ARRAY dest-in-rbx STRING_LIT**／**TYPE_SLICE dest-in-rbx STRING_LIT**／**TYPE_SLICE dest-in-rbx CAST**／**TYPE_SLICE dest-in-rbx IF**／**TYPE_SLICE dest-in-rbx BLOCK**／**TYPE_ARRAY dest-in-rbx CAST**／**TYPE_ARRAY dest-in-rbx IF**／**TYPE_ARRAY dest-in-rbx BLOCK**／**TYPE_NAMED dest-in-rbx CAST**／**TYPE_NAMED dest-in-rbx IF**／**TYPE_NAMED dest-in-rbx BLOCK**／**TYPE_NAMED dest-in-rbx FIELD／INDEX**／**VAR dest TYPE_NAMED CAST**／**VAR dest TYPE_NAMED IF**／**VAR dest TYPE_NAMED BLOCK**／**VAR dest TYPE_NAMED FIELD／INDEX**／**VAR dest TYPE_SLICE STRING_LIT**／**VAR dest TYPE_SLICE CAST**／**VAR dest TYPE_SLICE IF**／**VAR dest TYPE_SLICE BLOCK**／**VAR dest TYPE_NAMED CALL／METHOD**／guarded match／match subject CALL／INDEX／FIELD／const-int guard · 10.1.3 NT **暂缓** |

***

## 6. 变更记录（倒序 · **9 月 9 日**；8 日见 [归档](自举进度-归档-2026-09-08.md)，7 日及以前见 §1 归档行）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-09-10 | **test/docs(selfhost)** **7.4.4 v3 收尾：冷重建画布＋命名考古（`a58899348`）** | **测试＋文档** · **不升钉**。按知识卡姿势执行：①`./xbuild l4 --rebuild-only` 全量冷消航道混（4m52s 绿）——留确定态画布供 v3 单变量重试；②三验绿（矩阵 5/5／drift-gate 15/5/0／m2=m6=42）；③命名考古：基线裸 `-E` 的 8 个 `main_` 面系 main.x **字面函数名**（无隐藏前缀机制——上波「泄漏」系误判）；pin T 面为旧发射器**历史混合命名**（eq_asm 裸 vs main_driver_emit_* 带）→「`-E`＋注入」再生须逐名复刻旧命名（否决该捷径）；④原型实证：`-E`＋注入＋宽松 cc 可编译通过（rc=0 零 error），唯命名面不对。正路仍＝emit 航道 opt-in `-lib-name`（v3 知识卡）。 |
| 2026-09-10 | **docs(selfhost)** **7.4.4 v3 `-lib-name` 尝试波（回退收口）** | **文档** · **代码已回退** · 树复稳（矩阵 5/5＋drift-gate 15/5/0＋m6=42）。尝试＝opt-in `-lib-name` 前缀全链（BSS 槽／parse 双循环／三航道 setter＋Cap 层 entry 偏移 8389876/8390004／五 C 孪生／双 pin）——符号链路全通但输出零效果＋航道级联陈旧干扰（pabi/rt_emit_state/driver_x ensure 新鲜度洞）＋一次 T001 瞬态，按「一波一闭」纪律整体回退。**知识收获**：裸 `-x -E` 对 main.x 发射完整（唯一差异＝entry 前缀）；活跃航道＝`dispatch_x_emit_to_c`→`driver_run_x_emit_c` thin slice（非 run_x_emit_x）；警惕 rt_run_compiler_parsed writer 双写。重试姿势与完整地图入 C迁移追踪 7.4.4。 |
| 2026-09-10 | **fix(selfhost)** **7.4.4 v2 pin mtime 产生点根修（`28315bed2`）** | **代码＋文档** · **不升钉** · **脚本层（无产品面改动）**。v1 闸检出的陷阱在产生点杀死：driver_gen／preprocess／archaeology／lsp 四族 ensure 的 pinned 判定原先只看存在（+FORCE），git 改 pin 永不失效工作树 gen；现纳入 pin mtime——driver_gen 走 any_dep_newer 依赖集（-E 尝试→fallback seed），其余五处 pinned 需 seed 非新、pin 更新→恢复 seed 拷贝。migrate 族（parser/typeck/codegen/lexer）本就正确（seed -nt gen→刷新）未动。验＝双端复演昨夜陷阱（touch pin→无 FORCE ensure→`fallback seed`／`restored (pin newer)`／lsp 刷新）＋drift-gate 仍 15/5/0 绿；gen 字节相同无产品重建。v3 立卡＝.x↔pin 语义闸（**被 §3.1 `-E-extern` 发射缺口阻塞**：产品 rc=1/0 行，实测）＋CI 接线＋staged-diff 禁令。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.4.4 v1 pin↔gen 漂移闸（`34a87530d`）** | **代码＋文档** · **不升钉** · **构建级别：脚本闸（无产品面改动）**。选题＝Windows 卡阻塞（VM 离线无法拉起）后按 DAG 取 7.4.4。实现＝`compiler/scripts/pin_gen_drift_gate.sh`（G.7 单体）＋`./xbuild drift-gate` 入口：每对 pin seed↔工作树 gen 做 cc -c＋nm T 符号集对照；文本漂移（发射器版本格式化）不算、T 集漂移＝语义信号；缺 gen（按需构建 4 对）与考古退役（lsp_io_gen，wave1036）SKIP。验＝双端 15 PASS／5 SKIP 绿＋负样本（driver_gen.c 追加假符号→FAIL 报 only-in-gen）＋还原绿。直捕 2026-09-10 driver_gen 陈旧坑类（pin 改后工作树 gen 陈旧、mtime up-to-date 判定不感知）。v2 立卡＝.x↔pin 语义漂移（须产品 -E）／CI 接线／staged-diff 只改 seed 禁令。另：本波顺带实测 7.2.1 真实规模（seed 4,555 行＋切片族；pthin_*.x 仅 237 行→多波史诗，更新认知非 21,935 单文件）。PLATFORM: SHARED。 |
| 2026-09-10 | **test/docs(selfhost)** **双端 tip L4 真冷全绿 → 升钉 `e194cd294`→`fa1061224`** | **测试＋文档** · **升钉**。双端全擦冷启＠`fa1061224`：Ubuntu bootstrap＋g05＋矩阵 5/5＋bstrict **129/129**（7m46s）＝15m18s（`ubuntu-server:/tmp/xlang_l4_Linux_fa1061224.log`）；Darwin 同序全绿（bstrict 16m27s）＝21m50s（`/tmp/xlang_l4_Darwin_fa1061224.log`）。§3.3 收口后首钉。 |
| 2026-09-10 | **fix(selfhost)** **std/compress impl 前缀注入（`fa1061224`）** | **代码＋文档** · **升钉见上行** · **构建级别 L4**。根因（旧产品 A/B 定罪）：worktree 检出 `e194cd294` 造旧产品——旧 mod.x `-E` 的生成 C **仍含限定 import dep 的前缀函数体**（`std_compress_gzip_libz_compress_gzip_compress_c` 有 def）；`7e2fef61b`（skip std/ in need_coemit）停掉 std/ dep co-emit 后体消失，而车辆 impl 发射两 lane（`-lib-name ""` 与无 flag）**均裸名**（新旧产品 A/B 同裸——发射层非回归）→ mod.o U 前缀 vs T 裸错配。修＝G.7 补全车辆自有契约（「impl .x 无 `-lib-name` → 路径前缀」）：`xlang_compile_std_module.sh` 对 `std/compress/` 下 impl 源（**非 mod.x**——首版误注 mod 致双前缀，已守卫）的 `^export function` 名单在生成 C 上 perl 词界前缀化；名单动态取自源文件＝无第二套符号表；FFI extern "C" 名不在名单不动。验＝双端四 .o 重建（zlib T=4/U=0；gzip/zstd/brotli mod＋lib 面齐）＋`run-compress.sh` 双端 **compress test OK** ＋双端 L4 全绿升钉。运维记录：Darwin nm 前导下划线坑（`T _std_…` 须带 `_` grep）。PLATFORM: SHARED。 |
| 2026-09-10 | **fix(selfhost)** **link_alias 4 参 mangled 面＋L4 兜底双跑（`7f62aa9da`）** | **代码＋测试＋文档** · **不升钉（bstrict 新卡红）** · **构建级别 L4**。日终 L4 兜底首跑双端红在 seed-phase1 pure-ld：UNDEF `glue_try_std_heap_redirect_sym_local_u8_ptr_i32_u8_ptr_i32_reti32`（引用者＝prefer thin+rest 航道 `-E` 全量 dispatch.x 的 mangled X-ABI 调用）。根因＝`x_frontend_link_alias.from_x.c`（自述职责「backend_call_dispatch X-ABI mangled faces → C 产品符号」）对该函数仍是旧 3 参 weak 桩化石；昨日产品链绿纯靠生成式 `asm_full_link_stubs` 扫描偶然含面，L4 清场再生（扫描集无 dispatch）丢面→phase1／g05／CC 逃生口三连诚实红。修＝G.7 补全既有桥面（同胞 `glue_asm_build_func_export_sym_c_u8_ptr…` 同型）：4 参 mangled 转发替化石桩。验＝g05 恢复链绿（alias.o T-mangled＋U-plain）＋wave943 10/10＋矩阵 5/5；重跑双端 L4 **过 phase1／bootstrap（Ubuntu 329s／Darwin 199s）／g05／矩阵 5/5**（Ubuntu 14m33s／Darwin 21m11s 到 bstrict）。**新卡＝run-compress 冷链红（§3.3）**：双端 bstrict 唯一红，std/compress 子模块 .o lib 面裸名 vs mod.x 前缀调用错配（取证链完整：T=0/U=8、KEEP_C 裸名、探针分岔），下刀方向＝vehicle 层禁 bare（spec `mod|1`→compress 族自适应）。PLATFORM: SHARED。下一刀＝§3.3 根修→双端 L4 全绿升钉。 |
| 2026-09-10 | **fix(selfhost)** **parse 层悬空值吞噬守卫（`45f8296a5`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝parse 各层对取值 flag（`-o`／`-O`／`-L`／`-backend`／`-target`／`-target-cpu`）无条件 `i+2` 吞下一项——run 路径 `driver_argv_ensure_run_o` 注入的 `-o <temp>` 尾对被当值吃掉→产物未链 temp→execv ENOENT 127；存储＝parse state（out_path／lib_roots 等）；消费＝run／bare／build 全编译路径。修＝G.7 单权威新谓词 `driver_compile_argv_next_is_value_c`（rt_compile.x `#[no_mangle]`：下一项存在·非空·非 `-` 才吞；转发侧守卫的 parse 层孪生）；七文件同 commit 同语义——main.x `parse_x_path` 5支＋`parse_x` 3支＋`-E` 子循环、compile.x step 6支、rt_compile.x `step_c` 6支＋`apply_*_next_c` 6件、冷体 `rt_compile.from_x.c`、surface `-E` 风格、双 pin（`driver_gen`／`driver_compile_gen`）。**运维坑（立记录）**：`driver_gen.c` up-to-date 判定只看 `MAIN_X_DEPS` mtime、不感知 pin 变更——改 pin 后须 `XLANG_FORCE_REGEN_GEN=1 ./xbuild driver-gen` 再 `ensure_host_cc_seed_o try-heat driver_x.o`（Darwin／Ubuntu 双端同坑；首验 m6 绿曾靠第二层 parse 假象，nm U/T 三 .o 验证后才真全层就位）。验＝双端 wave943 **20/20**（m1–m5 回归＋五悬空形状 `hi -L`／`-backend`／`-target`／`-O`／`-target-cpu` 全 127→42＋悬空无位置参＝7＋bare）＋矩阵 5/5；g05 重建 driver_x.o／driver_compile_x.o／runtime_driver_no_c.o。PLATFORM: SHARED。 |
| 2026-09-09 | **fix(selfhost)** **9.4.3 v1 run 转发 flag 值表（`1c719831f`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝`xlang_driver_exec_spawn_wait`（seeds/runtime_driver_abi.from_x.c always-seed 单权威）扫描只特判 `-o` 对，`-O`／`-L`／`-backend`／`-target`／`-target-cpu` 的独立值泄漏进子进程 argv；存储＝static `cav[520]`（一次性 exec）；消费＝`xlang run`／bare exec 漏斗→编译程序 `main(argc, argv)`。修＝G.7 补全既有扫描：`xlang_driver_run_flag_consumes_value` 谓词对齐 parse 权威 i+2 面（main.x `driver_argv_parse_x_path`／`_x`＋rt_compile surface `-target-cpu`＋usage 表）；吞值守卫＝下一项存在且非 `-` 开头（悬空 flag 不吃注入 `-o` 尾对、temp 路径不泄漏）；附形 `-O1` 仍独立跳过。docblock 三面同 commit（seed＋`runtime_driver_abi.h`＋thin.x extern）。**立新卡＝parse 层悬空值吞噬**（compile 侧无条件 `i+2` 把注入 `-o` 当值吃掉→产物未链→execv ENOENT 127；`-L`／`-backend`／`-target`／`-O` 全中；m6＝复现探针）。验＝双端探针 m1–m5 **11/11**（Darwin `-target aarch64-apple-darwin`／Ubuntu `-target x86_64-linux-gnu` 宿主 triple；异 arch triple 合法跨发射 ld 拒≠债）＋m6 双端 127（卡复现）＋矩阵 5/5；g05 ensure+relink driver_abi（Darwin 23:38／Ubuntu 23:45）。PLATFORM: SHARED（POSIX fork/execv＋Windows spawnvp 共扫描）。下一刀＝parse 层悬空值守卫。 |
| 2026-09-09 | **fix(selfhost)** **fnptr-as thin 委托 `lea_fn_sym`（`392d44cb6`）** | **代码＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝first-wins `runtime_pipeline_abi_fnptr_as_thin.x` 仍内联 Mach-O `'_'`／ELF 裸名 LEA spell；mega 9.4.2 已委托 `pipe_modlet_lea_fn_sym_to_rax`，但产品 inject 吃 thin（Darwin mega -E OOM）。存储＝pabi `.o` first-wins `pipeline_asm_emit_as_elf_impl`。消费＝`fn as *u8`／TYPE_FN 产品 emit。修＝G.7 补全既有 thin：Cap-fn-ptr AS 转调权威；删 `95 as u8` 副本。禁第二套 spell；nest 冻 64／spawn 真体／seed WIN leftover addr_of 不动。验＝双端 fnptr_addr_smoke=42／fnptr_suite=42／thin_inline_spell=0＋wave942／addr_reloc／scalar_addr／p3 回归＋矩阵 5/5；g05 inject-only pabi（Darwin 23:19 T／Ubuntu 23:22 T impl＋W lea）；`backend_call_dispatch.o` 未动。PLATFORM: SHARED。下一刀＝9.4.3 v1。 |
| 2026-09-09 | **fix(selfhost)** **scalar ADDR_OF／fn-ptr 全局格 bake RELA（`94b9ba071`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝prepare 只注册 LIT／BOOL 与 TYPE_ARRAY+ARRAY_LIT，scalar `let p: *i32 = &g`／`let h: *u8 = inc` 跳过 modlet（entry hoist／seed only；非 hoist 函数读未播种槽＝100／138）；存储＝F7 `.data`＋reloc 表；消费＝库 TU／other() 读全局指针。修＝G.7 补全 prepare 注册＋hoist skip：谓词 `pipe_modlet_scalar_init_is_ptr_addr`（ADDR_OF 过 pass-1 cell／fn，AS／VAR 点同模 fn）；8B `.data`＋既有 `pipeline_elf_ctx_append_reloc_absolute64`。VAR 拷贝（`let g = f`）仍 hoist。禁第二套 reloc；nest 冻 64／spawn 真体不动。验＝双端探针 p_addrof=5／p_fn=42／p_as=42／p_call=42／p_copy=42／p_int=5／p_fn_section=data＋wave942／addr_reloc／p3 回归＋矩阵 5/5；g05 重建 pabi（Darwin 23:08／Ubuntu 23:10；须 `XLANG_HOST_CC_SEED_FORCE=1` 全量 thin+rest）；`backend_call_dispatch.o` 未动。PLATFORM: SHARED。下一刀＝fnptr-as thin。 |
| 2026-09-09 | **fix(selfhost)** **9.4.2 ADDR_OF／fn-ptr 全局表 bake RELA（`0a63ef5e7`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝prepare 用 `has_ptr_addr_elem` 把裸 fn／`fn as *u8`／`&global` 表挡进 COMMON（bake 曾不能表达 named-symbol reloc；库 TU 永不 seed＝指针 NULL）；存储＝F7 `.data`＋reloc 表；消费＝全局 fn-ptr／ADDR_OF 表。修＝G.7 补全 bake＋prepare：`pipe_modlet_bake_ptr_addr_elem_to_data` 走既有 `pipeline_elf_ctx_append_reloc_absolute64`；spell 抽到 `pipe_modlet_fn_sym_spell_into`（LEA 同权威）；`glue_fixed_array_total_bytes_c` TYPE_FN=18→8B（.x 相对 seed C 漂移，bake span 曾 CG002）。禁第二套 reloc；nest 冻 64／spawn 真体／scalar ADDR_OF 格不动。验＝双端探针 p_fn=42／p_as=42／p_addrof=12／p_call=42／p_int=42／p_fn_section=data＋wave942 回归＋矩阵 5/5；g05 重建 pabi（Darwin 22:37／Ubuntu 22:40）；`backend_call_dispatch.o` 未动。PLATFORM: SHARED。下一刀＝scalar ADDR_OF 全局格。 |
| 2026-09-09 | **fix(selfhost)** **parser slen>127 L011（`9bc09b934`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝parser 解码 `wi>=127` 粘 L011（pin `parse_body_lets_into`＋`parser_asm_primary_slice`）；jmp-skip `EB rel8` 另卡 126（127 parse OK 仍 CG002）。存储＝`Expr.var_name[128]`＋`int_val` 溢出链。消费＝jmp-skip／RELA intern／`glue_asm_string_lit_len`。修＝G.7 补全既有 var_name 存储：头 127 内联，溢出块经 `int_val` 链额外 STRING_LIT（帽 4095，超则仍 L011）；ident 槽 127 不动（4.2.8 leave-off）。emit 补全 `E9 rel32`＋RELA poke 走链。禁第二套字串池；nest 冻 64／spawn 真体不动。pin＋parser.x＋primary_slice 同 commit。验＝双端探针 p_127=0／p_128=0／p_200=0／p_adj=0／p_global=0／p_keep=L011＋p_long 回归＋矩阵 5/5；g05 重建 parser／pabi／dispatch（Darwin 22:13／Ubuntu 22:18）。PLATFORM: SHARED。下一刀＝9.4.2 ADDR_OF。 |
| 2026-09-09 | **fix(selfhost)** **p3 家族 `function(i32):i32` 全局赋值（`ab1c1fbf8`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝`typeck_x_ast_check_one_top_level_let` 等型闸漏 `typeck_fnptr_surface_compat`（模块级 `let f: function(i32): i32 = inc` T001 expected function found *u8）；局部 let／assign／`as`／TYPE_FN 表本已绿。存储＝模块 let type_ref＋expr resolved_type_ref。消费＝全局 TYPE_FN 格＋间接 call。修＝G.7 补全既有顶层 let 检查器：coerce＋整数拓宽后走既有 surface 闸（allow_opaque=0）。不开第二套 fnptr 检查。arity／ret／opaque Cap 仍 T001。pin 同 commit。empty_surface 无此 walker。验＝双端探针 p_global=42／local=42／as=42／tab=42／u8=42／copy=42／nullary=42／arity=T001／ret=T001／opaque=T001＋矩阵 5/5；g05 重建 typeck（Darwin 21:41／Ubuntu 21:43）；`backend_call_dispatch.o` 未动。PLATFORM: SHARED。下一刀＝parser slen>127 L011。 |
| 2026-09-09 | **fix(selfhost)** **Defect D C 整数提升（`76842942c`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝wave666 混合比较直拒（含 u8 vs i32 变量 T001＋XT001）；paren 复合左值 inside while 本已绿（产品 `while ((e)==k)`＋wave650 `while (e)==k`）；存储＝`typeck_check_expr_binop_cmp` 等型闸；消费＝while／INDEX／u8 变量比较。修＝G.7 补全既有 binop_cmp：lit coerce 后对 sub-int（u8／NAMED i8,i16,u16）走 C 整数提升 stamp→i32（权威 `typeck_cmp_promote_subint_to_i32`，复用 `typeck_integer_widen_ok_refs`）。不开 usual arith（i32 vs i64 仍 T001）／int↔bool／float／ptr。pin＋empty_surface 同 commit。验＝双端探针 u8==i32=0／ne=1／255=0／while=3／idx=0／wrap=0／lit=0／i32==i64=T001／i32==bool=T001＋矩阵 5/5；g05 重建 typeck（Darwin 21:29／Ubuntu 21:31）；`backend_call_dispatch.o` 未动。PLATFORM: SHARED。下一刀＝p3 家族 `function(i32):i32` 全局赋值。 |
| 2026-09-09 | **fix(selfhost)** **RELA＋字串池（`1fdec53e8`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝bake 只认 LIT、STRING_LIT 留 COMMON＋entry seeder（库 TU 永不 seed＝指针 NULL；jmp-skip 126 帽）；存储＝F7 `.data`／`g_pipe_elf_data_buf`；消费＝全局 `*u8[N]` lit。修＝G.7 补全 bake＋prepare use_data：STRING_LIT intern `.data` 池（`Lxmls_`＋NUL）＋`pipeline_elf_ctx_append_reloc_absolute64`（F7 vtable 同权威）。9.4.2 ADDR_OF 仍 COMMON。slen>127 仍 L011（parser 层）。禁第二套 emit／reloc；nest 冻 64／spawn 真体不动。验＝双端探针 p3=0／p2=42／p9=0／p_empty=0／p_long=0／pool_labels=2＋矩阵 5/5；g05 重建 pabi（Darwin 21:07／Ubuntu 21:11）；`backend_call_dispatch.o` 未动。PLATFORM: SHARED。下一刀＝Defect D。 |
| 2026-09-09 | **fix(selfhost)** **`x_stubs` 前缀 `asm_asm_codegen_ast` -1（`0133f91a0`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝`x_stubs` STRONG 前缀 ast return -1（experimental xlang_x）＋verify-selfhost 生成副本；存储＝ELF／Mach-O 先到先得／PE `--allow-multiple-definition` 先到先得；消费＝无 seed_bridge 则静默 -1／与真体同链则 multiply_defined 覆盖。修＝G.7 删两处 PREFIX -1 体（权威仍 `user_asm_seed_bridge` 强前缀 GAS＋peephole）。`experimental_symbol_bridge` WEAK PREFIX 转发保留（无前缀权威＝rt_asm_stub）。禁第二套 ast；Darwin Stage2 `filter_o_export` omit 留作陈旧 `.o` 防御；nest 冻 64／spawn 真体不动。验＝双端探针 leftover_prefix=0／bridge_first／provider_first direct=42 alias=42＋矩阵 5/5；双端产品 mtime 不变（`x_stubs` 不入 g05，Darwin 20:37／Ubuntu 20:39）；`backend_call_dispatch.o` 未动。PLATFORM: SHARED。下一刀＝RELA＋字串池。PREFIX elf_o／ast -1 leftover 至此全闭。 |
| 2026-09-09 | **fix(selfhost)** **strict_glue／`x_stubs` 前缀 `asm_asm_codegen_elf_o` -1（`62af44889`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝strict_glue `XLANG_WEAK` 前缀 elf_o return -1（产品 g05 `_GLUE_SUFFIX`）＋`x_stubs` STRONG 前缀 -1（experimental xlang_x）＋verify-selfhost 生成副本；存储＝ELF／Mach-O 先到先得／PE `--allow-multiple-definition` 先到先得；消费＝无 seed_bridge 则 CG002、档案预满足 U 抽不出真体。修＝G.7 删三处 PREFIX -1 体（权威仍 `user_asm_seed_bridge` 强前缀）。禁第二套 elf_o；Darwin Stage2 `filter_o_export` omit 留作陈旧 `.o` 防御；nest 冻 64／spawn 真体／`x_stubs` 前缀 ast -1 不动。验＝双端探针 leftover_prefix=0／bridge_first／provider_first direct=42 alias=42＋矩阵 5/5；双端 stubs.o 无 elf_o、产品 nm＝T 前缀真体。PLATFORM: SHARED。下一刀＝`x_stubs` 前缀 `asm_asm_codegen_ast` -1。 |
| 2026-09-09 | **fix(selfhost)** **postlink compiler-fallback 禁静默拷 `./xlang`（`b337b66b5`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝postlink 产品 smoke 失败后无条件 `cp "$FALLBACK"`（默认 `./xlang`）覆盖产品 `xlang_asm`（仅 `XLANG_BOOTSTRAP_NO_POSTLINK_FALLBACK=1` 硬禁）；存储＝产品二进制；消费＝postlink 覆盖 g05 金标，anti-collapse 事后才认。修＝G.7 补全既有 `XLANG_BOOTSTRAP_ALLOW_POSTLINK_FALLBACK` 到产生点；默认产品不动。experimental 拷仍走 `XLANG_EXPERIMENTAL_PROMOTE_TO_PRODUCT`。禁第二套 fallback 旗。验＝探针 gated=4／compiler_fallback_gated=1／ungated=0＋矩阵 5/5；产品 mtime 不变（脚本闸门，无 g05）。PLATFORM: SHARED。下一刀＝strict_glue／`x_stubs` 前缀 elf_o -1。 |
| 2026-09-09 | **fix(selfhost)** **Darwin keep／smoke／postlink 禁静默拷 experimental（`6449fa44d`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝Darwin strict-fail keep／smoke fallback／postlink experimental fallback 无条件 `cp` experimental→产品 `xlang_asm`（smoke 先拷再死仍污染）；存储＝产品二进制；消费＝build_xlang_asm／postlink／docker-ci 覆盖 g05 金标。修＝G.7 补全既有 `XLANG_EXPERIMENTAL_PROMOTE_TO_PRODUCT`（relink 权威＠`ddf74296e`）；smoke 本地 `XLANG_ASM_ALLOW_EXPERIMENTAL_FALLBACK`＝同一 opt-in；docker-ci 缺产品不再拷。禁第二套 promote。验＝探针 gated=4／ungated=0／docker_copy=0＋矩阵 5/5；产品 mtime 不变（脚本闸门，无 g05）。PLATFORM: SHARED。下一刀＝postlink compiler-fallback。 |
| 2026-09-09 | **fix(selfhost)** **退役 mega leftover sibling `_impl` 包装（`79bc22058`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝mega `src/runtime.x` 仍 export `driver_try_compile_via_shu_c_sibling` 转调从未定义的 `*_impl`；存储＝prove surface 100 T 锁；消费＝若 mega 再入链则 ELF／Mach-O 先到先得覆盖 `rt_dispatch_thin` 真体／UNDEF（调用点已闭＠`0c400d576`；产品权威＝thin → `driver_dispatch_sibling_try_spawn`，有 fork／exec 真体）。修＝G.7 删委托＋`*_impl` extern（.x＋surface 100→99 T）。**不删 spawn 真体**。禁第二套 spawn；nest 冻 64 不动。验＝双端探针 surface=0／product_sibling=1／product_spawn=1／product_impl=0＋prove runtime IDENTICAL＋邻域 mega／e05＋矩阵 5/5；双端产品 mtime 不变（mega 不入 g05，无 g05）。PLATFORM: SHARED。下一刀＝Darwin keep／smoke／postlink fallback。 |
| 2026-09-09 | **fix(selfhost)** **退役 mega leftover via_cparser `_impl` 包装（`62b4dd253`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝mega `src/runtime.x` 仍 export `driver_run_x_emit_c_extern_via_cparser` 转调从未定义的 `*_impl`；存储＝prove surface 101 T 锁；消费＝若 mega 再入链则 ELF／Mach-O 先到先得覆盖／UNDEF（调用点已闭＠`16dd9b577`；产品权威＝`driver_run_x_emit_c` → `driver_x_emit_try_extern_via_cparser` 恒 BLD001）。修＝G.7 删委托＋`*_impl` extern（.x＋surface 101→100 T）。禁第二套 emit；spawn 真体／nest 冻 64 不动。验＝双端探针 surface=0／product_try_extern=1／product_emit_c=1／product_retired=0＋prove runtime IDENTICAL＋邻域 mega／e05＋矩阵 5/5；双端产品 mtime 不变（mega 不入 g05，无 g05）。PLATFORM: SHARED。下一刀＝mega leftover sibling `_impl` 包装。 |
| 2026-09-09 | **fix(selfhost)** **退役 leftover `-E-extern` cparser 调用点（`16dd9b577`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝冷体 `rt_run_x_emit.from_x.c` `driver_run_x_emit_c` 仍在 `!XLANG_NO_C_FRONTEND` 下对 `-E-extern` 调 `driver_run_x_emit_c_extern_via_cparser`（mega 包装从未定义的 `_impl`，摘掉宏会 UNDEF）；存储＝`#ifndef FROM_X` 冷体；消费＝产品 PREFER `.x` 已走 `driver_x_emit_try_extern_via_cparser` 恒 BLD001。修＝G.7 删 cparser 调用＋未用 extern；冷体对齐 try_extern。禁删 mega via_cparser 包装；禁第二套 emit；spawn／nest 冻 64 不动。验＝双端探针 seed_cparser=0／product_try_extern=1／product_emit_c=1＋邻域 mega／e05＋矩阵 5/5；双端产品 mtime 不变（PREFER rest H=0，无 g05）。PLATFORM: SHARED。下一刀＝mega leftover via_cparser `_impl` 包装。 |
| 2026-09-09 | **fix(selfhost)** **退役 leftover `shu_c_sibling` 调用点（`0c400d576`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝冷体 `rt_dispatch_impl.from_x.c` `driver_run_emit_c_path_impl_c` 仍在 `!XLANG_NO_C_FRONTEND` 下对顶层 import fork／exec 同目录 `xlang-c`（`driver_try_compile_via_shu_c_sibling` **有真体**，摘掉宏会复活 C 前端而非 UNDEF）；存储＝`#ifndef FROM_X` 冷体；消费＝产品 PREFER `.x` 已直走 `driver_dispatch_run_compiler_parsed`。修＝G.7 删调用点＋未用 sibling／entry-only／generic extern（parsed 未用 sibling extern 一并删）。禁删 spawn 真体；禁第二套 typeck；`rt_run_x_emit` `-E-extern` 诊断／nest 冻 64 不动。验＝双端探针 seed_call=0／product_parsed=1／product_emit_c=1／product_sibling_body=1＋邻域 mega／e05＋矩阵 5/5；双端产品 mtime 不变（PREFER rest H=0，无 g05）。PLATFORM: SHARED。下一刀＝`rt_run_x_emit` leftover `-E-extern` 诊断。 |
| 2026-09-09 | **fix(selfhost)** **退役 leftover 泛型 lexer／parse `!XLANG_NO_C_FRONTEND` 块（`4c0ec6afe`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝冷体 `rt_run_compiler_parsed.from_x.c` 仍在 `!XLANG_NO_C_FRONTEND` 下 import 降级 `want_asm_backend=0` 并走 `lexer_new`／`parse`／`typeck_module`／`codegen_module_to_c`；存储＝`#ifndef FROM_X` 冷体；消费＝若摘掉 `XLANG_NO_C_FRONTEND` 则 UNDEF `lexer_new`（产品权威＝`rt_cp_step_try_c` → `driver_parsed_try_c_after_pp` 恒 -2＋`pipeline_typeck_entry_module`）。修＝G.7 删两块＋未用 extern／`token.h`。禁第二套 typeck；`shu_c_sibling`／nest 冻 64 不动。验＝双端探针 seed_c_lex=0／product_try_c=1／product_pipeline_typeck=1＋邻域 mega／e05＋矩阵 5/5；双端产品 mtime 不变（PREFER rest H=0，无 g05）。PLATFORM: SHARED。下一刀＝`rt_dispatch_impl` leftover `shu_c_sibling`。 |
| 2026-09-09 | **fix(selfhost)** **退役 `rt_run_*` leftover `!XLANG_NO_C_FRONTEND` 调用点（`a08d04d70`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝冷体 `rt_run_asm_backend.from_x.c`／`rt_run_compiler_parsed.from_x.c` 仍在 `!XLANG_NO_C_FRONTEND` 下调用已删 mega 的 `driver_c_frontend_smoke`／`driver_check_only_c_typeck`／`driver_c_typeck_entry{,_large_stack}`；存储＝未用 extern＋`#ifndef FROM_X` 冷体；消费＝若摘掉 `XLANG_NO_C_FRONTEND` 则 UNDEF（产品权威＝`driver_asm_try_c_frontend_early`／`driver_asm_try_c_typeck_precheck`＋`pipeline_typeck_entry_module`）。修＝G.7 删调用＋extern。禁第二套 typeck；泛型 lexer／parse 块／import 降级／nest 冻 64 不动。验＝双端探针 seed_call=0／product_try_c=1／product_pipeline_typeck=1／product_retired=0＋矩阵 5/5；双端产品 mtime 不变（PREFER rest H=0，无 g05）。PLATFORM: SHARED。下一刀＝`rt_run_compiler_parsed` leftover 泛型 lexer／parse 块。 |
| 2026-09-09 | **fix(selfhost)** **退役 mega stale `c_frontend_smoke`／`check_only_c_typeck`（`36bb731f1`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝mega `src/runtime.x` 仍 export `driver_c_frontend_smoke`／`driver_check_only_c_typeck` 转调从未定义的 `*_impl`；存储＝prove surface 103 T 锁；消费＝若 mega 再入链则 ELF／Mach-O 先到先得覆盖／UNDEF（C 前端已物理删除；产品 typeck 权威＝`pipeline_typeck_entry_module`）。修＝G.7 删委托＋`*_impl` extern（.x＋surface 103→101 T）。禁第二套 typeck；`rt_run_*` leftover `!XLANG_NO_C_FRONTEND` 调用点／nest 冻 64 不动。验＝双端探针 surface=0／product_pipeline_typeck=1／product_retired=0＋prove runtime IDENTICAL＋矩阵 5/5；双端产品 mtime 不变（mega 不入 g05）。PLATFORM: SHARED。下一刀＝`rt_run_*` leftover `!XLANG_NO_C_FRONTEND` 调用点。 |
| 2026-09-09 | **fix(selfhost)** **退役 mega stale `smoke_lex_dump`／`c_typeck_entry_*`（`023b26d09`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝mega `src/runtime.x` 仍 export `driver_smoke_lex_dump_{on_large_stack,thread_fn}`／`driver_c_typeck_entry{,_thread_fn,_large_stack}` 转调从未定义的 `*_impl`；存储＝prove surface 108 T 锁；消费＝若 mega 再入链则 ELF／Mach-O 先到先得覆盖／UNDEF（C 前端已物理删除；产品 typeck 权威＝`pipeline_typeck_entry_module`）。修＝G.7 删委托＋`*_impl` extern（.x＋surface 108→103 T）。禁第二套 typeck；`c_frontend_smoke`／`check_only_c_typeck`／nest 冻 64 不动。验＝双端探针 surface=0／product_pipeline_typeck=1／product_retired=0＋prove runtime IDENTICAL＋矩阵 5/5；双端产品 mtime 不变（mega 不入 g05）。PLATFORM: SHARED。下一刀＝其余 mega `_impl`（`c_frontend_smoke`／`check_only_c_typeck`）。 |
| 2026-09-09 | **fix(selfhost)** **`asm_codegen_elf_o` 5 参 -1 leftover（`81b2a6f98`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝`asm_experimental_symbol_bridge` 5 参 weak `asm_codegen_elf_o` return -1＋前缀弱别名转发进该 -1；存储＝Darwin USER_ASM_LINK 含 bridge.o（Linux 产品链不含）；消费＝无前缀调用得 -1／缺 seed_bridge 则前缀名 CG002。修＝G.7 删 -1 体＋只转发到它的前缀弱别名（产品权威＝`user_asm_seed_bridge` 强前缀；无前缀真体、保留别名会 UNDEF Darwin g05）。禁第二套 elf_o；nest 冻 64／其余 mega `_impl` 不动。验＝双端探针 bridge_first／provider_first direct=42 alias=42＋矩阵 5/5；Darwin 产品 nm＝强前缀真体、无前缀 -1 已消失；Ubuntu 产品 mtime 不变。PLATFORM: SHARED。下一刀＝其余 mega `_impl` 委托。 |
| 2026-09-09 | **fix(selfhost)** **退役 mega stale esc_gate（`9ae7d8349`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝mega `src/runtime.x` 委托 `driver_stack_esc_gate_{thread_fn,large_stack}` → 从未定义的 `*_impl`；存储＝prove surface 110 T 锁；消费＝若 mega 再入链则 ELF／Mach-O 先到先得覆盖 `rt_stack.x` 真体。修＝G.7 删委托＋`*_impl` extern（.x＋surface 110→108）；权威仍 `rt_stack.x`。禁第二套 esc_gate；其余 mega `_impl`／nest 冻 64 不动。验＝双端探针 surface=0／rt_stack=2／product=4＋prove runtime IDENTICAL＋矩阵 5/5。PLATFORM: SHARED。下一刀＝`asm_codegen_elf_o` 5 参 -1 leftover。 |
| 2026-09-09 | **fix(selfhost)** **bridge 4 参 weak 桩先序（`2eb6aaa83`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝`asm_experimental_symbol_bridge` 4 参 weak `asm_codegen_ast` return -1；存储＝ELF／Mach-O 同名先到先得；消费＝覆盖 rt_asm_stub 3 参 GAS（Darwin 产品 nm＝`mov w0,#-1`）。修＝G.7 删该 -1 桩（与同文件 typeck／parser 同构）；前缀 alias 仍弱转发；缺真体→UNDEF。禁第二套桩。验＝双端探针 bridge_first／provider_first direct=42 alias=42＋矩阵 5/5；Darwin `asm_codegen_ast` 现为强 3 参 GAS。PLATFORM: SHARED。下一刀＝退役 mega stale esc_gate。 |
| 2026-09-09 | **fix(selfhost)** **modlet 表 64 项上限（`213ca9ec5`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝`pipeline_asm_modlet_prepare_and_emit_elf_c` 满 64 静默 `break`；存储＝`g_pipeline_asm_modlet` n 停 64；消费＝load／store miss → 栈槽（9.6.0 双家园类）。修＝G.7 补全既有表 MAX 64→256（偏移由 `pipe_modlet_max` 派生；seed `XLANG_ASM_MODLET_MAX`）＋注册前高声拒 return -1→CG002（跳过项不绊帽）。禁第二套可增长表；nest 冻 64 不动。验＝双端 n64／n65／n256／plus_const run=2、n257 CG002＋矩阵 5/5。PLATFORM: SHARED。下一刀＝bridge 4 参 weak 桩先序。 |
| 2026-09-09 | **fix(selfhost)** **experimental promote 链（`ddf74296e`）** | **代码＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝`relink_xlang_asm_experimental_bootstrap` 无条件 `cp` experimental→产品 `xlang_asm`；存储＝产品二进制；消费＝WPO ensure／`ensure_experimental_ast_pool_for_wpo` 覆盖 Ubuntu 金标。修＝G.7 补全既有 relink 只写 experimental；escape `XLANG_EXPERIMENTAL_PROMOTE_TO_PRODUCT=1`。禁第二套 promote。验＝双端矩阵 5/5；产品 mtime 不变（Darwin 15:58／Ubuntu 17:17）。残＝Darwin strict-fail keep／smoke fallback／postlink fallback。PLATFORM: SHARED。下一刀＝modlet 表 64 项上限。 |
| 2026-09-09 | **fix(selfhost)** **Ubuntu WPO `main.o` CG002（`564278d29`）** | **代码＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝`driver_abi_pctx_off_asm_entry_module_only` 8389808（`prefix_mirror[64]` 旧值）写入 mirror；存储＝pctx.asm_entry_module_only；消费＝seed_bridge 读 pipe 8389872＝0 → 共发射 codegen.x → CG002 `codegen_emit_async_run_seed_push_name`。修＝G.7 补全既有 off→8389872（header／ast.x／pipe 锁步）。禁第二套 ENTRY 门。验＝Ubuntu `main.x` ENTRY_MODULE_ONLY rc=0 T=25（无 codegen T）＋双端矩阵 5/5。Darwin 冷 seed 本已 0x8004f0。PLATFORM: SHARED LP64。下一刀＝experimental promote 链。 |
| 2026-09-09 | **fix(selfhost)** **Darwin wpo-full-chain ensure（`6e4478892`）** | **代码＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝块注释含 `*/`（`i64*/u8*`／`submit_*/wait`／`free_*/realloc_*`）或乱码未闭 `/**`；存储＝WPO dogfood `-backend asm` 源；消费＝lexer L003／L001 → ensure 五模块硬死。修＝G.7 补全既有注释（不开第二套 WPO／lexer）。验＝Darwin ensure＋chain 5/5、full-chain status=ok run=3 obs=1 skip=2（strict_link UNDEF 仍 obs）＋矩阵 5/5；Ubuntu 矩阵 5/5。残＝Ubuntu WPO `main.o` CG002 ELF。PLATFORM: SHARED 注释；DARWIN ensure 硬死。下一刀＝Ubuntu WPO `main.o` CG002。 |
| 2026-09-09 | **fix(selfhost)** **host-C `va_arg<i32>` 单实例（`4ca63e4cd`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝`codegen_mono_combo_slot_equal` 只认 TYPE_NAMED，builtin i32／f64／PTR 不同 type_ref 不去重；存储＝mono combo 表；消费＝host-C 同 TU 重定义 `va_arg__VaList_i32`。修＝G.7 补全既有转调 `codegen_type_refs_same_for_mono`（.x＋seed）。禁第二套 va 路。验＝双端 LANG-VA-CAP-BUILTINS run=1（五份 i32 一 def＋mixed 两份 f64 host-cc 42）＋VA-CAP run=1＋矩阵 5/5。PLATFORM: SHARED host-C。下一刀＝wpo-full-chain Darwin 红（MSVC 须切 Windows）。 |
| 2026-09-09 | **fix(selfhost)** **10.7.1 va gate 双端绿（`f35b03fe1`）** | **代码＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝smoke `export extern va_start` 被 `-E` 原样打成 C 重声明＋变参 extras 无 formal 不走 SSE＋AAPCS64 va 游标误用 x86 `rbp-off` -8＋cleanup 按 nargs-reg_max；存储＝extern 声明／xmm 槽／header 游标／SP；消费＝Darwin clang host-cc／Ubuntu asm rc=4／Darwin mixed PC=0。修＝G.7 补全既有 `codegen_is_libc_conflicting_extern_name`←`typeck_is_cap_va_builtin_name`、`glue_arg_ref_is_sse_float_c` extras、aarch64 +8／GE、cleanup＝FP 感知 n_stack。禁第二套 va 路。验＝双端 LANG-VA-CAP-BUILTINS run=1＋VA-CAP run=1＋矩阵 5/5。PLATFORM: SHARED skip-emit／SSE；MACOS／ARM64 x29+off。下一刀＝host-C `va_arg<i32>` 同符号。 |
| 2026-09-09 | **docs(selfhost)** **macOS f-atomic 5 dup 诚实收口（无新代码）** | **文档** · **不升钉** · **构建级别 L2**。五问：产生＝Darwin ld64 曾叠 catalog `atomic.o` T `std_atomic_*` 与共发射／二次入链；存储＝`std/atomic/atomic.o`＋ld argv；消费＝ld64 无 `-multiply_defined`（GNU ld 曾藏）。现场＝既有 `pipeline_codegen_std_dep_link_only` 含 `std.atomic`（user.o U）＋`link_abi_asm_ld_argv_has_obj` realpath 去重；glue 只 T `atomic_*_c`。禁第二套 atomic 桥／禁改 C11 glue。验＝Darwin FORCE 重建 atomic.o＋额外 CLI `.o` 0 dup；双端 `run-f-atomic-v1-gate` static/ensure/glue/ordering/widen 全 1＋矩阵 5/5。PLATFORM: DARWIN 历史脸／SHARED 权威已在。下一刀＝10.7.1 va gate。 |
| 2026-09-09 | **fix(selfhost)** **prove `runtime_pipeline_abi` surface regen＝2044 T IDENTICAL（`f8156c1c6`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝wave575 surface 锁 270 T，mega `.x` -E 已 2044 T；存储＝`runtime_pipeline_abi_surface.from_x.c` 手术补丁不 Regen；消费＝prove nm-diff 1833 行。修＝G.7 按既有 Regen 契约 prove-aligned `-E`（DBG 滤＋prologue＋`pipe_*` rename）重生 surface；2 枚 surface-only 桩（`pipeline_fill_soa_field_access_for_asm_emit`／`pipeline_module_fixup_with_arena_stmt_orders`）非 `.x` -E T 面、不回写。禁第二套 prove。验＝Darwin／Ubuntu T 集 SHA 相同；双端 prove IDENTICAL＋矩阵 5/5。PLATFORM: SHARED。下一刀＝macOS f-atomic 5 dup。 |
| 2026-09-09 | **fix(selfhost)** **Ubuntu compress stream process＝link_only（`661f2970c`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝`asm_asm_codegen_elf_o` 共发射不咨询 `link_only`＋表仍写「无 compress.o」；存储＝user.o T 盖 gzip.o；消费＝`s.hdr.inited` movq@0 冲 magic、process n<0。修＝G.7 补全既有 `pipeline_codegen_std_dep_link_only`（`std.compress`＋`std/compress` 路径、c_face `compress_state_bytes`＠`21c84fdc5`）＋发射循环咨询该表（`661f2970c`）。禁 host-cc `compress/mod.x`；禁第二套 skip。验＝双端 family/cb/unified=0、user.o U（不再 T）、矩阵 5/5＋run-compress。PLATFORM: SHARED。下一刀＝macOS prove `runtime_pipeline_abi` 符号差。 |
| 2026-09-09 | **fix(selfhost)** **compress stream c_face 转发（`7e2fef61b`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝c_face init/process/end `-1`；存储＝compress.o 桩 T；消费＝cookbook init≠0 跳过假绿。修＝G.7 补全既有 c_face 转发子模块 stream T＋L8b `compress_init` 针（`1677323cf`）；lib.x cap 改字面量破 co-emit 短名递归（`538efe6a3`）；need_coemit 补 `std/`（`7e2fef61b`）。禁 host-cc `compress/mod.x`；禁第二套 stream。验＝Darwin family/cb=0＋矩阵 5/5＋run-compress；Ubuntu 矩阵 5/5＋run-compress、family=2（仍 co-emit process n<0）。PLATFORM: SHARED。下一刀＝Ubuntu stream process。 |
| 2026-09-09 | **fix(selfhost)** **u64 关系比较无符号 setcc（`1adfbaa58`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝cmp 宽度回退已盖 `==`／`!=`，关系比较仍走有符号 setcc；存储＝`glue_emit_cmp_finish` cc 0..5；消费＝u64 `t=2^63` 的 `t>0` 当负数。根修＝G.7 补全既有 `glue_binop_operand_is_unsigned_elf_c` 进 finish：cc 2..5→6..9（x86 setb/seta；ARM64 cset lo/hi；RISC-V sltu；.x＋seed 孪生）。禁第二套宽度路。验＝双端 family/dep/len=0＋i64 仍 signed（Darwin cset hi/lo vs gt/lt；Ubuntu seta/setb vs setg/setl）＋矩阵 5/5＋run-compress。PLATFORM: SHARED。下一刀＝compress stream 面。 |
| 2026-09-09 | **fix(selfhost)** **zstd/brotli 子模块 catalog＋伴推（`e0b5013b4`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝c_face zstd/brotli `return -1`＋catalog 无子模块 `.o`＋sg15 不 push＋L8b 针只有 C API 前缀（user.o facade 打不着 `-l*`）；存储＝compress.o 桩 T；消费＝`tests/compress/main.x` n≤0 跳过假绿。根修＝G.7 补全 gzip 同路：catalog mod+lib、c_face 转发 `std_compress_{zstd_zstd,brotli_brotli}_*`、sg15 伴推、L8b/seed facade 针 2→10。禁 host-cc `compress/mod.x`；禁 Linux-only 绝对 `.so` 进 heavy.x。验＝双端 zs/br probe=0＋矩阵 5/5＋run-compress（Darwin `libzstd.1`＋`libbrotlienc/dec`；Ubuntu `libzstd.so.1`＋`libbrotlienc/dec.so.1`）；g05 `L8b+L8c` `.x` split。残＝stream 面仍 `-1`。PLATFORM: SHARED。下一刀＝u64 其余比较族。 |
| 2026-09-09 | **fix(selfhost)** **`xlang build` on-demand gzip 推送面（`189eb9b08`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝c_face gzip `return -1`＋heavy.x sg15 只 ensure 不 push＋asm ld `compress_o=NULL`；存储＝compress.o 桩 T；消费＝`std_compress_gzip_compress` n≤0 跳过假绿。根修＝G.7 补全既有 c_face 转发 `std_compress_gzip_gzip_*`＋sg15 伴推 zlib.o/gzip.o（≡ encoding）＋L8b/seed facade 针（user.o 开火 `-lz`）。禁扩 typeck skip。验＝双端 gzip probe=0＋矩阵 5/5＋run-compress（Darwin `libz.1.dylib`）；g05 `L8b+L8c` `.x` split。残＝zstd/brotli catalog。PLATFORM: SHARED。下一刀＝zstd/brotli 子模块 catalog。 |
| 2026-09-09 | **fix(selfhost)** **mac scratch 点分 dep 共发射＝UN `_core_*`（`8236ed6d8`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝`pipeline_asm_user_deps_need_coemit` 把一切 `core.*` 当 hosted entry-only；存储＝`asm_entry_module_only`；消费＝seed_bridge 跳过 dep 共发射→ld UN `_core_m6_*`。根修＝G.7 补全既有谓词（仅 in-tree `core/` 12 名跳共发射；scratch `core.m6` 共发射）＋seed_bridge 混编仍跳 hosted core（禁 duplicate）。禁扩 `skip_asm_user_core_lib`（那是 typeck 四名，扩了会退布局／父链）。验＝双端 `-o`/`-c` T `core_m6_add2` run=42；foo.bar=42；option=102；hello=0；L2 5/5＋run-compress。PLATFORM: SHARED。下一刀＝`xlang build` on-demand 推送面。 |
| 2026-09-09 | **fix(selfhost)** **L8c extern 面＝heavy.x fn typeck／-E 发射（`d3f0a2feb`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝wave956／958 壳调用 L8b `needs_std_{thread,vec,http}`＋`labi_od_rel_{vec,http}` 无 L8c extern；存储＝未解析 CALL type_ref=0；消费＝check_block 静默失败→L8c `-E` 死→prefer full-seed。根修＝G.7 补全既有 extern 表（函数体不动）。验＝`-E` typeck OK 且发射 T；g05 `L8b+L8c` `.x` split；双端 L2 5/5＋run-compress。PLATFORM: SHARED。下一刀＝mac scratch 点分 dep。 |
| 2026-09-09 | **fix(selfhost)** **compute_frame_size scratch 512→2048＝Darwin invoke_cc（`8281d5777`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝`pipeline_asm_compute_frame_size_c` min scratch 512 低估 emit next_offset；存储＝AsmFuncCtx frame_size；消费＝X 编 `invoke_cc_append_argv_head_flags` 写 x29+0x898 越过 0x6d0 帧砸 C `xlang_invoke_cc_impl` c_paths。根修＝G.7 补全既有安全网 512→2048＋w157 CALL (n+1)*32＋ADDR_OF ko==51（.x＋seed 孪生）。禁改 impl／fmt／c_paths。验＝Darwin `-backend c -o` probe_rc=0 run=42 帧 0x6d0→0x8f0；Ubuntu 同探针 run=42 帧 0x8e8；双端 L2 5/5。PLATFORM: SHARED。下一刀＝heavy.x fn#142。 |
| 2026-09-09 | **test/docs(selfhost)** **双端 tip L4 真冷全绿 → 升钉 `d583f61f5`→`e194cd294`** | **测试＋文档** · **升钉**。双端全擦冷启＠代码 SHA `e194cd294`：Ubuntu remaining_o=0→bootstrap 357s→g05 111s→L2 5/5→bstrict **129/129** 451s＝15m33s（`/tmp/xlang_l4_Linux_e194cd294.log`）＋Darwin remaining_o=0→bootstrap 206s→g05 94s→L2 5/5→bstrict **129/129** 997s＝22m7s（`/tmp/xlang_l4_Darwin_e194cd294.log`）。晨间钉 `d583f61f5` Darwin 侧曾把 bstrict 管道进 tail -3 假绿；本波为诚实双端全绿。下一刀＝Darwin invoke_cc SEGV。 |
| 2026-09-09 | **fix(selfhost)** **Darwin dir_open `O_DIRECTORY`＝fmt 收集见文件（`e194cd294`）** | **代码＋测试** · **升钉见上行** · **构建级别 L4**。五问：产生＝Darwin `xlang_dir_open` `SYS_open(O_RDONLY)` 无 `O_DIRECTORY`（Linux Cap 已有）；存储＝非空 DIR*；消费＝`fmt_path_stat_kind` 把文件当目录→walk 空表 FMT001。根修＝G.7 补全权威头 `O_RDONLY`＋`O_DIRECTORY`（0x00100000）；禁改 fmt-wrap／`fmt_path_stat_kind`。验＝host-cc 文件=NULL／dir 可读＋dir-cap Darwin gate＋fmt `/tmp/*.x`＋fmt-wrap 5/5＋L2 5/5＋双端 L4 129/129。PLATFORM: MACOS／DARWIN。 |
| 2026-09-09 | **test(selfhost)** **Ubuntu tip L4 真冷 ✅＠`074544410`；Darwin L4 红 fmt-wrap FMT001** | **测试＋文档** · **不升钉（缺 Darwin）** · **构建级别 L4**。Ubuntu `./xbuild l4`：remaining_o=0→bootstrap 352s→g05 111s→L2 5/5→bstrict **129/129** 451s＝**15m27s**；日志 `/tmp/xlang_l4_Linux_074544410.log`。Darwin 同入口：seed-smoke 经 -E+cc `-I` 过；bstrict 死在 run-check／fmt-wrap（冷 fmt 对任意 `.x` 零收集）。晨间 Darwin「L4 绿」曾把 bootstrap 管道进 tail -2 吞 smoke FAIL。 |
| 2026-09-09 | **fix(selfhost)** **seed-smoke -E+cc 补产品 -I（`074544410`）** | **代码** · **不升钉**。Darwin `-backend c -o` SEGV `xlang_invoke_cc_impl`＋pin egg 同崩；回退 `-E`+cc 缺 `compiler/include`→`xlang_va_cap.h` not found。补 `-I. -Iinclude -Isrc`。invoke_cc SEGV 另卡。 |
| 2026-09-09 | **fix(selfhost)** **L8b g15 seed count 24→28＋c_face 4 T（`51327d07b`／`e6c8d28f2`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝L8c prefer 失败时 full-seed first-wins；存储＝`labi_od_simple_group_sym_count(g==15)` seed 仍 24（针 i==24..27 已在）；消费＝CNT15=24→不推 compress。假绿＝`grep return 28` 命中 g9 slice。根修＝seed 孪生 28＋c_face 补 `std_compress_{gzip,brotli,zstd}_stream_state_bytes`／brotli init＿＋`.so` 从 slice 块挪回 compress 伴块。**heavy.x 不动**。验＝双端 L2 5/5＋run-compress 绿；Ubuntu 链上 `mov $0x1c`。下一刀＝tip 双端 L4。 |
| 2026-09-09 | **fix(selfhost)** **cmp 宽度 dep 回退 — u64 `==` 64 位（`65d32efb5`）＋L8b 非确定性新卡** | **代码＋测试＋文档** · **不升钉**。五问：产生＝cmp 宽度只认 typeck stamp（dep 无）；存储＝`pipeline_asm_emit_cmp_elf` is_cmp_64bit；消费＝u64 `t==0` 32 位比较（旧「u64 实参到 0」卡实为此；实参 movabs 正确）。根修＝VAR stamp 空时接 `glue_var_expr_type_ref_with_decl_fallback_c`（binop-shift 同链）左右两面（.x＋seed）。验＝dep eq64lit/clz64(2^63)/use2 全绿＋双端 L2 5/5＋mac 十门禁绿含 run-compress；**⚠️ Ubuntu run-compress 红＝L8b 航道非确定性新卡（§3.2，晨间同配方零红）**；重链次序敏感（须完整 build relink）。 |
| 2026-09-09 | **test/docs(selfhost)** **双端 tip L4 真冷全绿 → 升钉 `b5be5ed97`→`d583f61f5`** | **测试＋文档** · **升钉**。双端全擦冷启：Ubuntu（bootstrap 5m54s→all 2m32s→L2 5/5→bstrict **129/129 零红** 5m59s；`/tmp/xlang_l4_Linux_d583f61f5.log`）＋mac（同序全程 16m、bstrict 零红 9m48s；`/tmp/xlang_l4_Darwin_d583f61f5.log`）＝**bstrict 零红历史首次即钉**。本日累计：三连闭（slice/process）→run-compress→L4×2→升钉。 |
| 2026-09-09 | **test(selfhost)** **Ubuntu tip L4 真冷 ✅＠`d583f61f5`；mac L4 后台启动** | **测试 only** · **不升钉（待双端）**。全擦 compiler/std/core 全部 `.o`＋四二进制→`bootstrap-driver-seed` rc=0（5m54s）→`XLANG_HOST_CC_SEED_FORCE=1 xbuild all` rc=0（2m32s）→L2 5/5→**全量 bstrict 129/129 零红（5m59s）**＝tip 首次 L4 零红。日志 `/tmp/xlang_l4_Linux_d583f61f5.log`。mac L4 09:15 后台起（历史 137m）；双端绿→升钉。 |
| 2026-09-09 | **fix(selfhost)** **g15 stream 针＋系统 .so 推送 — run-compress 绿＝全量 bstrict 129/129 零红首次（`d583f61f5`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。落点＝双活航道：g15 表 24→28（L8b `.x` 切片）＋seed 伴块 `.so` 推送（L8b full-seed 后备）；**heavy.x 不动**（-E 清了翻 split 雷区）。验＝**Ubuntu 全量 bstrict 129/129 零红（8m26s 首次）**＋十二门禁绿＋L2 5/5；mac 同套门禁绿（含 run-compress）＋L2 5/5。 |
| 2026-09-09 | **docs(selfhost)** **sg15 推送两轮受阻于 labi 多切片谜（`ce571abb9`＋`6141bf3e4`）** | **文档 only** · **不升钉** · **无产品面改动**。sg15 补推 zlib.o/gzip.o＋系统 `.so`（.x＋seed）后 `runtime_link_abi.o` 丢 `xlang_asm_ld_append_on_demand_user_objs` 定义→g05 断链。第二轮取证：pure-asm 切片对 heavy.x 恒败（既有 T001，fn #139）＝常态走 `-E` 后备；基线合并件 542T/defs=1 vs 编辑件 552T/defs=0，三中间件两轮均 0 defs＝定义者另有来源（L0..L9 mktemp 或合并阶段）未捕获；字面量直传非元凶。已回退保双绿。下一刀＝保留 L0..L9 切片找定义者真身（§3.1）。 |
| 2026-09-09 | **docs(selfhost)** **run-compress 建图＝asm 直链航道第三方库接入（诊断 only）** | **文档 only** · **不升钉** · **无产品面改动**。五问：产生＝`-l*` 旗标只挂 C 前端车道；存储＝zstd/brotli lib.x extern FFI；消费＝asm 直链 raw ld 无旗标面→UNDEF。g15 针表已扩＋触发实证；zlib 先例＝X 移植；替代＝推系统 .so 绝对路径。下一刀二选一（§3.1）。 |
| 2026-09-09 | **fix(selfhost)** **三连闭＝run-slice＋run-process 双端绿（`f7e9a16a7`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。①u64 步长：elem 恢复 `<8`→`≤8`（`[]u64` stride 4→8，last_u64 读错元）；②core_slice ensure：wipe 删 slice.o→needle 静默跳过→UNDEF；照 sys.o 纪律 ensure_formal_make（wipe 实测自愈）；③run-process（昨日被 run-slice 中止遮蔽）：co-emit std.sys.linux 定义 wrapper 留 FFI `xlang_sys_*` U＝真定义者在 `compiler/src/asm/freestanding_io_x86_64.o`（仅主编链）→sys 针表 34→38＋need_any_sys 补推 fio（.x＋seed 双孪生）。验＝双端 run-slice/run-process/set/string/bitops/env/net/heap/channel/json/stdtest 全绿＋L2 5/5；bstrict 单红 run-compress（zstd/brotli 未建）。 |
