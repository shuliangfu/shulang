# ast/ — 抽象语法树

**AST 节点定义**与**树遍历**相关代码。

- **内容**：各类 AST 节点（表达式、语句、函数、模块等）的数据结构；可选：访问者/遍历工具。
- **职责**：parser 构建 AST 时使用本目录定义；typeck、ir 等读取 AST，不修改语法结构定义。

流水线位置：parser 产出 AST，后续 typeck 与 ir 消费 AST。
