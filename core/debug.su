// core.debug — assert、debug 打印等（自举前最小实现）
// 自举前：assert(b) 在条件为假时 panic；返回 0 便于作为表达式使用。

function placeholder(): i32 { return 0; }

// 条件为假时终止程序（调用 panic）；条件为真时返回 0。if-else 分分支 return，可读性更好。
function assert(b: bool): i32 {
  if (!b) {
    return panic();
  } else {
    return 0;
  }
}
