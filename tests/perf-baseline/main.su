// 阶段 8 性能基线：固定工作量（循环 + 整数运算），便于 -O2/-Os 前后对比
// 累加和 0+1+...+9999999 = 49999995000000 用 i64 存储，main 返回 i64（C 侧截断为 int）
function main(): i64 {
  let n: i64 = 0;
  for (let i: i32 = 0; i < 10000000; i = i + 1) {
    n = n + i;
  }
  return n;
}
