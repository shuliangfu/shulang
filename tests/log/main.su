// 测试 std.log：set_min_level、log(level, ptr, len)
import std.log;

function main(): i32 {
  set_min_level(level_info());
  let msg: [5]u8 = [111, 107, 32, 49, 0];
  if (log(level_info(), &msg[0], 4) != 0) { return 1; }
  return 0;
}
