# 分析文档目录不提交
analysis/

# --- 构建产物 ---
# 可执行文件（编译器 shu / shu_su / shu-c 等）
shu
shu_su
shu-c
su
*.exe
*.out

# C 目标文件与静态库
*.o
*.obj
*.a
*.lib
*.so
*.dylib
*.dll

# 链接与中间文件
*.ll
*.bc
*.s
*.asm

# 构建时生成的 C 源文件（由 -E 等生成，不提交）
std_fs_gen.c

# sed -i.bak 等产生的备份，不提交
*.bak

# --- 构建系统 ---
# CMake 生成文件
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
*.cmake
build/
out/
# 仅忽略构建目录内生成的 Makefile，根目录 Makefile 可提交
build/Makefile
out/Makefile

# --- 依赖与包 ---
# 第三方 / 子模块（若后续引入）
vendor/
third_party/
deps/
.pkg/
# Node（VSCode 扩展 editors/vscode 等）
node_modules/

# --- 开发环境 ---
# IDE
.idea/
.vscode/
.cursor/
*.swp
*.swo
*~
.project
.settings/
*.sublime-*

# OS
.DS_Store
Thumbs.db
*.log

# --- compiler 目录：构建产物与生成文件（不提交） ---
# 可执行文件（根已有 shu/shu-c，此处补 compiler 下特有）
compiler/shu_driver
compiler/build_tool
# 构建目录
compiler/build_asm/
compiler/build/
compiler/su/
# 由 -E 或脚本生成的 C 源文件（parser_gen.c, driver_gen.c, pipeline_gen.c, typeck_gen.c, codegen_gen.c 等）
compiler/*_gen.c
compiler/**/*_gen.c
compiler/std_fs_gen.c
# 备份与临时
compiler/*.new
compiler/*.bak
compiler/*.append
# 调试与临时
compiler/scripts/lldb_crash_bt.txt
compiler/test_perl.txt

# --- 测试与临时 ---
# 测试 ABI 脚本编译出的可执行文件
tests/abi/layout_abi
tests/tmp_out.txt
# 测试输出、临时目录
tmp/
temp/
*.tmp
*.temp
.cache/

# --- 可选：调试与覆盖率 ---
# *.dSYM/
# coverage/
# *.gcno
# *.gcda
